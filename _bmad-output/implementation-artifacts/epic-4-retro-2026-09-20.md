---
epic: 4
date: 2026-09-20
verdict: rejected
criteria: declared
headless: false
---

# Epic 4 Retrospective: Pipeline Validation Milestone (CLI End-to-End)

## Epic summary

- **Epic:** 4, Pipeline Validation Milestone (`epics.md:643`, stories at `epics.md:1831-2300`). The gate is Story 4.10: Part A green in CI, plus Part B recorded in `Tests/fixtures/epic4-exit-results.md`.
- **Diff range:** `fc1c08a^..9510507`. `fc1c08a` is the first Story 4.1 commit. `9510507` is the merge of PR #87 (Story 4.10). The range holds 100 commits, 46 of them merges (34 measured by `git_evidence.py`). Non-epic commits are mixed in (Epic 1 to 3 retro fixes in Persist, State, Core, and one vault-glossary test deflake). They are not attributed to any story.
- **Change volume:** `Sources/` +7305, `Tests/` +11461, `App/` +503, `_bmad-output/` +3692. Largest additions by module: Attribute 1038, Orchestrator 816, Persist 673 (retro fixes), Core 522, Transcribe 445, Pipeline 423, ReviewDiarization 390.
- **Stories:** 9 of 10 are `done` by their spec frontmatter (4.1 to 4.9). Story 4.10 is `in-progress`: `spec-4-10-exit-criteria-gate-ci-pipeline-test-live-run.md` records Part A done and Part B awaiting the maintainer.
- **Unfinished stories (`pending_stories`):** `detect-epic --epic 4` lists all 10 keys because `sprint-status.yaml` still shows every Epic 4 story as `backlog`. That file is stale, and AGENTS.md names each spec's `status` as ground truth. The retro treats the specs as authoritative. By the specs one story is unfinished (4.10), and the machine verdict is **rejected** either way.

### Evidence inventory

| Evidence | Available | Source |
|---|---|---|
| Epic spec with declared exit criteria | yes | `epics.md:2263` (Story 4.10) |
| Epic context | yes | `epic-4-context.md` |
| Story specs | yes, 10 | `spec-4-*.md` |
| Diff range, per-story commits | yes, partial attribution: only 11 of the 54 non-merge commits name a story in the subject | `git_evidence.py` |
| Sprint status | yes, stale for Epic 4 | `sprint-status.yaml` |
| Previous retrospective | yes | `epic-3-retro-2026-09-18.md` |
| Session logs | not gathered | process lessons come from spec review records and merge order |
| Behavior check | yes | `swift test --explicit-target-dependency-import-check error`: 1328 tests in 13 suites, exit 0 |
| Live run (Part B) | **no** | needs private recordings, WhisperKit and live Anthropic calls |

## Findings

The diff-scope findings come from one read-only review pass over the cross-story seams. Findings 1 and 2 were re-checked by the retro against the source. Findings 3 to 6 are **reported** and rest on the review pass alone. Disposition key: **fix** = action item, **defer** = tracked, **accept** = recorded.

### F1. Diarization never reaches the published note (blocker, fix)

- **Spans:** 4.1/4.2 (transcribe, diarize) with 4.6/4.7/4.10 (attribute, run, exit test).
- **Evidence (verified):** `Sources/WhisperKitTranscriber/WhisperKitTranscriber.swift:23` labels every utterance `Speaker_1`, and `transcript.json` is immutable. `Sources/Summarize/SummaryArtifactMapper.swift:26` maps names by `speakers[utterance.speakerLabel]`. `renderTranscript` (`Sources/Attribute/AttributionRenderer.swift:48`) is defined but no code outside `Attribute` calls it. `utteranceIndex` is read only in the renderer and the reviewer prompt.
- **Effect:** `auricle run <id> --speakers "1=Ben,2=Sara"` attributes every utterance to one person and `needsAttribution` reads false. Part B would measure a pipeline that cannot distinguish speakers, which defeats the wedge thesis the epic exists to test.
- **Why tests passed:** `Tests/IntegrationTests/IntegrationStubs.swift:55` builds a transcript that is already labelled per speaker, so the join is never exercised.
- **Upstream cause:** no story owned the join from `diarization.json` and `attribution.json` to the labels Summarize reads. 4.2 produces, 4.6 renders, 4.7 wires, and none of the ACs says who consumes.

### F2. `--publish-anyway` note carries no `auricle/needs-attribution` tag (fix)

- **Evidence (verified):** `SummaryArtifactMapper.needsAttribution` (`:55-59`) only tests that each transcript label is a key of `speakers`. A publish-anyway file maps `Speaker_N` to itself, so it reads as attributed. `spec-4-6-*.md:13` records the intended fix. PR #87 states the same finding.
- **Overlaps with F1:** the fix needs the same label source.

### F3. `--from` and `--only` skip the state machine (fix, reported)

`Sources/Orchestrator/RunPlan.swift:173-176` rejects only terminal and permanent-failure states. `StageRunner.run` writes the `started` transition without `expectedState`. Example: `--only notify` on an `awaiting_attribution` meeting moves it to `awaiting_verification` with no note.

### F4. Re-transcribe leaves stale artifacts that attribute reuses (fix, reported)

`--force` and `--from transcribe` rewrite `diarization.json`. `Sources/Attribute/AttributionStage.swift:149-150` reuses the old speakers map, overrides and splits after checking only that the map is non-empty. A re-run of review overwrites `diarization_suggestions.json`, which the epic context says is written once.

### F5. `--publish-anyway` discards a saved attribution draft (defer, reported)

`AttributionStage.swift:131-133` ignores `inputs.existing`. The 4.6 and 4.7 specs say only "placeholders", so this is unspecified rather than wrong. It needs a product decision.

### F6. Smaller items (defer, reported)

- Re-running review overwrites cost with 0 and model `"flag_off"` (`StateStore.swift:321` upsert, `ReviewDiarizationStage.swift:127-137`).
- A finished transcribe leaves state `transcribing`, and a failed review start re-runs the whole transcribe (`TranscribeStage.swift:119`, `RunPlan.swift:196`).
- Interrupt handling builds failure metadata inline instead of using `StageRunner`'s builder (`PipelineRunner.swift:290-300`).

### F7. Known deferred items carried from stories (defer)

- `InternalStageWorker` `--publish-anyway` wiring has no test, because the worker body lives in `App/` (`deferred-work.md`, Story 4.7).
- A `published_partial` meeting skips notify and `awaiting_verification` (Story 4.7).
- Five Story 4.2 items: transcribe retry repeats transcription after a diarize failure, partial SpeakerKit model download never repaired, `MonoAudioLoader` end-of-stream flush (unverified), `.multiple`/`.noMatch` segments dropped (unverified), no test of a successful SpeakerKit load.

### F8. Sprint status stale for Epic 4 (fix)

All ten Epic 4 keys read `backlog` although nine specs are `done`. Repeats the Epic 3 finding.

### Done well

- `Sources/Orchestrator/InternalStageArguments.swift`: one type builds and parses subprocess argv, with an exhaustive stage switch.
- `Sources/ClaudeAIReviewers/ClaudeDiarizationReviewer.swift:59-110`: invalid suggestions are dropped before they are written. Config falls back to flag off (`ReviewDiarizationSettings.swift:31`).
- `expectedState` guards on sweep and interrupt writes in `StateStore` and `StageRunner`.
- Story 4.10's PR reported its own finding (F2) and its unrun Part B honestly.

## Behavior verification

`swift test --explicit-target-dependency-import-check error` passed: 1328 tests, 13 suites, exit 0. That includes Part A (`PipelineEndToEndTests`, two scenarios). The real binary was **not** run end to end, and no real recording went through WhisperKit. F1 shows that green tests do not cover the diarization-to-note join.

## Previous-retro follow-through

`action_items` in `sprint-status.yaml` holds 31 entries from the Epic 1, 2 and 3 retros. All 31 read `done`. No entry from an earlier epic is open, so there is no status change to propose. Evidence checked: the sprint-status file only. The retro did not re-verify each landed fix.

## Action items

Owners: "maintainer" runs it, "story" is a new story-shaped fix for the dev loop. All are proposals.

1. **Fix the diarization-to-speaker join (F1, F2), story.** Give the join an owner: Summarize input labels come from `diarization.json` plus `attribution.json`, not the immutable transcript. Make `needsAttribution` treat a `Speaker_N` value as unmapped. Blocks item 3.
2. **Add an e2e test with a real-shaped transcript (F1), story.** Every utterance `Speaker_1`, a multi-speaker `diarization.json`, and an assertion that the note names two speakers. Part of item 1.
3. **Run Story 4.10 Part B, maintainer.** Only after item 1. Record the result in `Tests/fixtures/epic4-exit-results.md`.
4. **Guard `--from`/`--only` and re-runs (F3, F4), story.** Pass `expectedState` on the `started` transition, reject impossible starts, and clear or validate `attribution.json` when `diarization.json` is rewritten.
5. **Move `InternalStageWorker` body into `Sources/` behind a thin wrapper (F7), story.** Add the missing `--publish-anyway` wiring test.
6. **Repair `sprint-status.yaml` for Epic 4 (F8), maintainer.** Use the `bmad-sprint-planning` fix flow.
7. **Decide F5 and the `published_partial` notify question, maintainer.** Both are product decisions.
8. **Triage the small items in F6 and the Story 4.2 deferrals, maintainer.** Later, not blocking.

### Process lessons

- **Give every artifact join an owner story.** The epic listed producers and renderers but no story consumed the diarization output for summarization. Add "who reads this artifact" to the AC checklist when a story writes a new artifact.
- **Stubs must match real stage output.** The Part A stub pre-labelled speakers, which hid F1. Build stub transcripts from the real transcriber's shape.
- **Stories merged out of numeric order** (4.9 before 4.3, 4.8 after 4.6). Nothing broke, but the boundary checks belong to the last merge. Note the merge order in the epic context.

## Acceptance verdict

**Rejected** (declared criteria). Evidence:

1. Story 4.10 is `in-progress`. Part B has not run, and `Tests/fixtures/epic4-exit-results.md` records no "Epic 4 exit criteria met" line.
2. F1 is a blocking defect: the shipped pipeline cannot tell speakers apart, so the exit measurement would test a broken path.

This is the machine verdict for an unfinished epic, not a judgment on stories 4.1 to 4.9. All nine are `done` and CI is green.

## Open questions

- Should a `--publish-anyway` re-run keep a saved attribution draft (F5)?
- Should a `published_partial` note fire the notifier (Story 4.7 deferral)?
- Was F1 known and intended to be closed by Epic 7's GUI? The epic context defers overrides and splits to Epic 7, but not the base speaker-to-utterance mapping. The specs do not say either way.
