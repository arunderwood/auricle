---
epic: 3
date: 2026-09-18
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 3 Retrospective: Quote-Grounded Summarization Engine

## Epic summary

- **Epic:** 3, Quote-Grounded Summarization Engine (`epics.md:627`, stories at `epics.md:1336-1810`).
- **Diff range:** `6450e81^..bbad063`. `6450e81` is the first story commit (Story 3.1). `bbad063` is the last (Story 3.10, PR #41). The range holds 45 commits, 16 of them merges. `git_evidence.py` measured 15 of the 16 merges.
- **Range contamination:** the range also holds non-epic commits. They are `1ecba0b` (CLAUDE.md), `56d9d96` and `edbb3ff` (Dependabot), `266c5bc` (WhisperKit and Yams bump), `0d42c15` and `38a070f` (write-contention test deflake), and `6d284d4` (`CacheArtifactWriter`, a Story 1.2 carry-over).
- **Change volume:** 202 files touched. `Sources/`: 82 files, +6347/-707. `Tests/`: 90 files, +18578/-741, of which about 8,400 added lines are eval transcript fixtures. `App/`: 5 files, +351/-113. `_bmad-output/`: 16 files, +2739/-5. No binary revisions.
- **Stories completed (12 of 12):** every `spec-3-*.md` file has `status: 'done'`. Story 3.7 has two specs (`spec-3-7-summarize-stage-entry-point-cache-dir-handoff.md` and `spec-3-7-close-out-telemetry-blockquote-cross-stage-test.md`). `spec-cache-artifact-writer.md` is a Story 1.2 carry-over.
- **Unfinished stories (`pending_stories`):** `detect-epic --epic 3` reports all 12 Epic 3 keys as `backlog` in `sprint-status.yaml`. That file's `last_updated` is 2026-09-16, before any Epic 3 code landed, so it is stale. The user's AGENTS.md names each spec's own `status` as ground truth. Assumption recorded here: the retro treats the specs as authoritative, and the machine "rejected" verdict that the stale file would force is not applied. Sprint-status staleness is a finding below. Later the same day the tracker was repaired with the `bmad-sprint-planning` fix flow, and `detect-epic --epic 3` now returns `pending_stories: []`, so the gate no longer needs the override.

### Evidence inventory

| Evidence | Available | Source |
|---|---|---|
| Epic spec with declared goals | yes | `epics.md:1336`, `epics.md:627` |
| Story specs | yes, 12 stories in 13 spec files | `_bmad-output/implementation-artifacts/spec-3-*.md` |
| Story baselines in spec frontmatter | yes | All 12 Epic 3 specs record `baseline_revision`. Story 3.1's value equals `6450e81^`, the left edge of the range used here. The retro derived the range from git history and did not use the per-story baselines. |
| Diff range and per-story commits | yes, partial attribution | `git_evidence.py`. Only 3.1 to 3.9 name their story in a commit subject. Later commits are attributed by PR branch and spec file. |
| Sprint status | yes, stale | `sprint-status.yaml` |
| Previous retrospective | none | no `epic-*-retro-*.md` exists |
| Session logs | not gathered | process lessons come from spec review records and commit order only |
| Decision record and research | yes | `decision-record-2026-09-16-epic-3-ai-invocation.md` |
| Smoke-test results | yes | `Tests/fixtures/smoke-test-results.md` |
| Behavior check | yes | `swift test --explicit-target-dependency-import-check error`, exit 0, 675 tests |

## Findings

The aggregate views were derived by the retro. The diff-scope group comes from two read-only review passes over the epic diff (core summarizer, and calendar plus glossary). Each finding in that group is marked **verified** when the retro re-read the cited source, or **reported** when it rests on the review pass alone. Only verified findings became action items.

Disposition key: **fix** = fix now (becomes an action item), **defer** = tracked with context, **accept** = recorded so later retros stop re-flagging it, **decide** = needs a maintainer decision before a disposition exists.

### Spec-to-implementation reconciliation

- **SR-1 (major, fix).** The planning documents still describe Citations as the primary strategy with a substring fallback. The as-built default is substring with no fallback.
  - Planning text: `prd.md:515` (FR32), `prd.md:727`, `architecture.md:1360`, `architecture.md:2683`, `architecture.md:2824`, `epics.md:276` (AR-SUM-2), `epics.md:629`, `epics.md:1498`, `epics.md:1546-1571`.
  - As built: `App/auricle-cli/Verbs/InternalStageWorker.swift:84` builds `SummarizerOrchestrator(primary: ClaudeSubstringSummarizer())`. `Tests/fixtures/smoke-test-results.md` (Outcome) records the flip.
  - Story 3.8 already allowed this outcome (`epics.md:1636`), but it has no task to update the plan when the default flips.
  - Prevention: a lock-in story carries a spec-reconciliation task.
- **SR-2 (major, fix).** Story 3.12's vault glossary never reaches a production summarize run. `--vault-path` is an option of `InternalStageWorker` (`InternalStageWorker.swift:32`, `:116`). `SubprocessDispatcher.makeProcess` does not pass it (`Sources/Orchestrator/SubprocessDispatcher.swift:38`, and no `vault-path` string in `Sources/Orchestrator/`). A dispatched run gets an empty glossary, so the wedge measurement reports 0 eligible corrections (`spec-3-12-…md`, `deferred` entry, severity medium).
  - Root cause: `Sources/Core/Config.swift` does not exist. Story 1.2 deferred it and named Epics 5 and 9 as its first consumers (`deferred-work.md`, the Config entry). Epic 3's glossary is an earlier consumer, so that reasoning was wrong. TOMLKit is already a dependency (`Package.swift:50`).
  - This entry is absent from `deferred-work.md` (see PL-3).
- **SR-3 (major, decide).** `GoogleCalendarSource` (Story 3.10) is not constructed anywhere outside its own module. `App/Project.swift:30` links the product. A text search of `Sources/` and `App/` finds no reference outside `Sources/GoogleCalendarSource/`. `InternalStageWorker.swift` has no calendar reference. The stage consumes a `CalendarSource` protocol (Story 3.11), and only tests supply an implementation, so calendar enrichment is off in the live pipeline. The cause is a circular deferral. Story 3.10 says "Don't wire the source into `SummarizeStage`, `InternalStageWorker`, `AuricleApp` or any composition root … Those belong to Story 3.11" (`spec-3-10-…md:49`). Story 3.11 says "the composition root keeps passing no source" (`spec-3-11-…md:38`) and "every real run takes the degraded path until Story 3.10 lands and wires `GoogleCalendarSource`" (`spec-3-11-…md:194`). No story owns the wiring. `deferred-work.md` has no entry about calendar. The only planned re-auth trigger is the Story 9.1 Settings button (`epics.md:3590`). `GoogleCalendarSourceTests` has no dependency on `Summarize` (`Package.swift:157`), so no test runs the real source through the stage.
- **SR-4 (minor, accept).** Stories 3.8 and 3.9 planned for at least 5 of the maintainer's own recordings (`epics.md:279` AR-SUM-5, `epics.md:1339`, `epics.md:1614`). The comparison and eval set are public fixtures instead: four AMI meetings and two movie scenes. `smoke-test-results.md` records the maintainer's acceptance. Real-meeting validation of the wedge stays open for Epic 4.
- **SR-5 (minor, accept).** Story 3.8's AC wires the default in `AuricleApp.swift` and the CLI (`epics.md:1636`). Only the CLI is wired. The app has no summarize path until Epic 4/6.
- **SR-6 (minor, accept).** `smoke-test-results.md` records that Citations was not re-measured after the shared prompt was tightened, and that the maintainer has not spot-checked the recall and false-keep scores. The Citations strategy (`ClaudeCitationsSummarizer.swift`, 333 lines, plus tests) ships unwired, as the AC requires. The flip itself rests on computed metrics (Citations returned no items on 5 of 6 transcripts), not on the hand scores.
- **SR-7 (medium, decide).** Two eval fixtures are dialogue from commercial films, committed to a public repository. `Tests/SummarizeTests/Fixtures/eval/NOTICE.md` (Movie scenes) says the maintainer supplied them and confirmed they may be committed, and that "this repository does not verify their license independently". `gh repo view` reports the repository as public. The fixtures are `office-space-interview` (2,199 characters, 25 utterances) and `margin-call-boardroom` (6,519 characters, 50 utterances). The retro cannot judge the licensing question. The maintainer already made the call, so the finding is only that the risk is recorded in one line of a NOTICE file and nowhere in a decision. The four AMI fixtures are CC BY 4.0 with attribution and are not affected. Options if the maintainer revisits: remove the two scenes, or replace them with synthetic or licensed scenes. The comparison results cite `office-space-interview` and `margin-call-boardroom` by name, so a replacement needs a re-run to keep the record honest.

### Diff-scope review

**Verified by the retro (source re-read):**

- **DR-1 (major, fix).** Speaker labels duplicate in the rendered note. `SummaryArtifactMapper.transcriptSegments` slices each utterance's whole `[start,end)` range, prefix included (`Sources/Summarize/SummaryArtifactMapper.swift:20-24`). `FrontmatterRenderer` then prints `**\(speaker):** \(text)` (`Sources/Persist/FrontmatterRenderer.swift:121`). The result reads `**[[Speaker_1]]:** Speaker_1: …`, and after attribution `**[[Ben]]:** Speaker_2: …`. Tests pin the doubled shape (`Tests/SummarizeTests/SummaryArtifactMapperTests.swift:52-53`). The review reports that the eval fixtures' ranges include the prefix. Whether real transcripts do is undefined until Story 4.1 (`deferred-work.md`, utterance-range entry). Spec 3.7 rejected the concern as `[false]` (`spec-3-7-…md:156`), and the code path shows it is real for any transcript whose ranges include the prefix.
- **DR-2 (major, fix).** `effortLevel` never reaches the API. The only readers are two telemetry writes (`Sources/Summarize/SummarizeStage.swift:202`, `:309`), and nothing under `Sources/ClaudeSummarizer/` reads it. Story 3.4 chose this on purpose: "leave it unused" because no AC fixed the mapping (`spec-3-4-…md:46`). No later story owns the mapping. NFR-I6 makes `medium` the default and configurable (`prd.md:674`), and NFR-C1 names "reduce effort toward `low`" as a cost knob (`prd.md:706`). Telemetry records `medium` for every meeting whatever ran. Prevention: a spec that declares a config field "unused" names the story that will use it.
- **DR-3 (medium, fix).** Failure classes hide the cause. `sourceTranscriptQuote` is a non-optional `String` (`Sources/ClaudeSummarizer/ClaudeSubstringSummarizer.swift:13`), so one item without a quote fails the whole paid call as malformed. `stopReason` is parsed (`AnthropicHTTPClient.swift:34`, `:87`) and read nowhere. The comparison history shows a `max_tokens` truncation on `ami-es2002b` and 12 of 12 arm failures as `malformedResponse` (`smoke-test-results.md`, Run history). A missing API key throws `KeychainError.notFound` (`Sources/ClaudeSummarizer/KeychainAPIKey.swift:55`), which lands as `summarize_unexpected_error` (`SummarizeStage.swift:337-341`), so a first run has no actionable error class.
- **DR-4 (major, fix before wiring).** The event matcher can pick the wrong meeting. It tests only `capture_started_at` against inclusive event bounds and takes the shortest match, with no lead-in tolerance (`Sources/GoogleCalendarSource/EventMatcher.swift:21-38`). Recording at 9:59:30 for a 10:00 meeting matches a 9:00 to 10:00 meeting that is still on the calendar. The request `fields` mask omits `responseStatus`, `eventType` and `transparency` (`Sources/GoogleCalendarSource/GoogleCalendarSource.swift:34`), so a declined event or a short focus block can win. The review reports that a non-nil match also removes the `needs-calendar-enrichment` tag, so the wrong event looks authoritative (`CalendarEnrichment.swift:58`, `SummarizeStage.swift:191-193`; not re-read) and that `EventMatcherTests.swift:51` cannot tell "shorter" from "later start" (not re-read). Latent until SR-3 is fixed.
- **DR-5 (minor, defer).** Calendar failure reasons are lost. A missing source, no match, an expired credential and an outage all return the same `.degraded` artifact, with a `log.warn` as the only trace (`Sources/Summarize/CalendarEnrichment.swift:44-57`). The review reports that a Google project in Testing status expires refresh tokens after 7 days (`spec-3-10-…md` Design Notes; not re-read), so notes would degrade silently with nothing for a doctor check to read. Relevant to Story 9.2.
- **DR-6 (minor, defer).** The loopback listener burns its single-use slot before it checks `state`. `accepted = true` is set as soon as any request carries OAuth query items (`Sources/GoogleCalendarSource/LoopbackRedirectListener.swift:115-119`), and `state` is checked later in `GoogleOAuthFlow.swift`. A local process sending `GET /?error=x` fails the sign-in. The `state` check still blocks code injection (reported).
- **DR-7 (minor, fix).** Comparison reports are written world-readable. `detail.md` and `results.md` are mode 0644 on disk (`ls -l Tests/fixtures/strategy-comparison-output/20260918T234148Z/`). The writer uses `AtomicWriter` directly (`Sources/Summarize/StrategyComparisonReportWriter.swift:25-26`), and `CacheArtifactWriter` sets 0600 (reported). The output directory is gitignored (`.gitignore:240`). Today's contents come from public fixtures. A run over private transcripts would write meeting text at 0644.
- **DR-8 (minor, defer).** Dead surface after the fallback removal. The stage resolves a prompt hash for every `SummarizationMode`, so the unused Citations prompt must still resolve or the stage fails with `promptSetUnavailable` (`SummarizeStage.swift:274-284`). The review reports that `isFallbackEligible`, `Outcome.fallbackTriggered` and `SummarizeMeta.fallback_*` are reachable only from tests (not re-read).

**Reported by the review, not re-verified.** Recorded so nothing is lost. None became an action item. Re-check each before relying on it.

- **RV-1 (plausible).** The retry budget counts sleep time only, and no request timeout is set. The retro confirmed both facts by search of `AnthropicHTTPClient.swift`. The consequence is unconfirmed: a slow non-streaming response could be retried while the 720 s stale sweep marks the stage failed (`Sources/Orchestrator/StageRunner.swift:115`).
- **RV-2 (verified behavior, plausible impact).** `SubstringGroundingValidator` accepts the first match with no minimum length (`Sources/ClaudeSummarizer/SubstringGroundingValidator.swift:20`), so a short quote can ground to an earlier utterance by another speaker. `smoke-test-results.md` already reports quotes that "support only part of an item".
- **RV-3 (plausible).** Telemetry and `refreshMeetingRow` run after `summary.json` is written (`SummarizeStage.swift:186-207`, `:233-238`). A busy database there marks the meeting `summarization_failed` after a paid call, and a retry pays again.
- **RV-4 (plausible).** `SecItemCopyMatching` runs synchronously inside the lookup bound (`Sources/GoogleCalendarSource/GoogleRefreshTokenStore.swift:24-33`). A Keychain access prompt in the headless worker could outlast the 15 s bound.
- **RV-5 (plausible).** Attendee `displayName` is capped and filtered for email addresses only (`CalendarEnrichment.swift:67-79`), so long or instruction-like names reach every prompt. Glossary scoping takes attendees from `attribution.json`, not from the calendar (`SummarizeStage+Glossary.swift:15-18`).
- **RV-6 (known).** An item's `text` with a line break breaks its bullet. Already in `deferred-work.md`. `authorize()` can hang if the listener stays in `.waiting` (recorded only in the `spec-3-10-…md` frontmatter).

**Checked and clean** (from the two review passes, plus the retro's own reads):

- OAuth and PKCE: 32-byte random verifier, S256 challenge, `state` checked before `error`, literal `127.0.0.1` redirect, single-use listener cancelled on every exit, TLS 1.2 minimum, HTTPS precondition on all endpoints, no token or title in any log call (reported by the calendar pass).
- Byte-offset arithmetic in `SubstringGroundingValidator` and `TranscriptSlicer` agrees on UTF-8 bytes, empty quotes are dropped, bad ranges fail without trapping (reported by the core pass).
- Primitives: the review found no inline `stage_events` or `telemetry` write, no file write outside `AtomicWriter`, and no direct `print` or `os_log` in the slice. Cache artifacts use explicit snake_case `CodingKeys` (reported).
- Privacy: the API key is read per request and never stored or logged. Log fields carry no content. `calendar.json` holds no email addresses (reported).
- The stage validates input and resolves prompt hashes before the paid call, and cancellation is handled (reported).
- Not examined by either pass: `Sources/Persist` beyond the cited lines, `Tests/CalendarInterfaceTests`, `FuzzyTermIndexing` internals, and the unused `VaultGlossary` dependency of `ClaudeSummarizer` (`Package.swift:99`).

### Non-functional requirement check

- **NF-1 (medium, decide).** One measured call exceeds the default cost ceiling. NFR-C1 sets at most $0.50 per meeting on the default tier, for a 30-minute meeting (`epics.md:226`). The comparison run recorded $0.5682 for `ami-es2002b` with the original prompt, an empty glossary and no attendee context (`smoke-test-results.md`, Computed metrics). That run used substring, now the default. The length of `ami-es2002b` was not established, so the retro cannot say whether the ceiling was breached or the meeting was longer than 30 minutes. The tuned prompt was not re-costed per transcript, only in total ($9.82 across three runs). The mean over the six substring calls is about $0.295. The plan assigns "default tier validation" of NFR-C1 to this epic, "empirically held in smoke test + eval harness" (`epics.md:582`, `epics.md:637`). `smoke-test-results.md` never states whether the ceiling held. No code compares recorded cost to the ceiling: `remainingCostBudgetUSD` is declared (`Sources/SummarizerInterface/SummarizerConfig.swift:10`) and read nowhere. The PRD says NFR-C1 "is not advisory" (`prd.md:707`). The maintainer must say whether the ceiling is per meeting or in aggregate (Epic 4's exit criterion says "in aggregate", `epics.md:655`).
- Checked and clean: prompt caching is wired. `cache_control` is applied in `Sources/ClaudeSummarizer/ClaudeSubstringSummarizer.swift:178-190`, which satisfies AR-SUM-5 (`epics.md:279`) and Story 3.2's cache-marker AC (`epics.md:1393-1395`). Story 3.4's residual risk ("`promptCachingEnabled` unwired") no longer holds.

### Verification gap

- **VG-1 (major, fix).** The regression harness tests the wrong composition. `EvalDefaultWiring` (`Tests/SummarizeTests/SummarizeEvalHarnessTests.swift:68-77`) builds Citations primary with a substring fallback, and asserts `!fallbackTriggered` and `groundingMethod == .citations` (`:36-37`). Production builds substring only (`InternalStageWorker.swift:84`). Its doc comment says it "mirrors the provisional wiring" and that the enum is "the one place the harness changes" when the default locks (`:64-66`). The default locked in `ad39f6d` and this enum did not change. All 675 tests pass, so the harness reports green on a composition nobody ships.
  - Story 3.9's own deferred entry named this exact risk (`spec-3-9-…md`, `deferred`, "EvalDefaultWiring mirrors InternalStageWorker and no test ties them"). The proposed closure was to move the wiring into `Sources/` once 3.8 locks the default. The condition is now met.
  - The Citations stub is a hand-authored response shape (`EvalStubResponses.swift`), and the live comparison attached no citation objects (`smoke-test-results.md`, Rationale). The shipped composition (`SummarizerOrchestrator` with a nil fallback and default `SummarizerConfig`) has no test at all.
  - Note: the harness stubs never read the request, so prompt tuning (`2c612e8`) is not regression-guarded. The comment at `SummarizeEvalHarnessTests.swift:9-11` says so.

### Architecture delta

- **AD-1 (minor, decide).** Two edges in `Package.swift` differ from the graph in `architecture.md:2592` and `:2598`.
  - `ClaudeSummarizer` now depends on `Summarize` and `VaultGlossary` (documented: `Core, SummarizerInterface`). The strategies import the stage target (`Sources/ClaudeSummarizer/ClaudeSubstringSummarizer.swift:3`, `ClaudeCitationsSummarizer.swift:3`) because `SummarizationPromptBuilder` lives in `Summarize`.
  - `Summarize` now depends on `Orchestrator` and `AIReviewerInterface` (`Sources/Summarize/GlossaryJargonCorrector.swift:1`). The documented edges omit both.
  - Consequence: a future local-LLM strategy target (FR33) would also depend on the stage target. `architecture.md:175` says the dependency inversion is "enforced by manifest"; no check compares `Package.swift` to the documented graph.
  - Options: move `SummarizationPromptBuilder` and its `Prompts/` resources into `SummarizerInterface` or a new target, or update the documented graph. Decision belongs to the maintainer.
- Checked and clean: no dependency cycle in `Package.swift`. The largest source file after the epic is `Sources/Summarize/SummarizeStage.swift` at 357 lines. No file passes 400 lines, so there is no god-class.

### Duplication map

- **DM-1 (minor, defer).** Six test files use `URLProtocol` stubs: `Tests/SummarizeTests/CrossModeFixtureTests.swift`, `Tests/SummarizeTests/EvalStubResponses.swift`, `Tests/ClaudeSummarizerTests/AnthropicHTTPClientTests.swift`, `ClaudeSubstringSummarizerTests.swift`, `ClaudeCitationsSummarizerTests.swift`, and `Tests/GoogleCalendarSourceTests/GoogleStub.swift`. Story 3.5 deferred consolidating the first four (`spec-3-5-…md`, `deferred`). Stories 3.9 and 3.10 each added another. Whether the two newer stubs duplicate the older four was not verified.
- Checked and clean: no near-duplicate of `CacheArtifactWriter`, `AtomicWriter`, `StageEventLogger` or `TelemetryRecorder` was found in the epic's new files by name search only. The diff-scope review covers inline bypasses of these primitives.

### Pattern divergence and naming

- **PD-1 (minor, fix).** Naming drift after a mid-epic rename. Commit `ef7a0ac` renamed the rig from "smoke test" to "strategy comparison" and the code followed (`StrategyComparison*`, `run-strategy-comparison.sh`). The story spec is still `spec-3-8-smoke-test-rig-scaffold.md`, the results file is still `Tests/fixtures/smoke-test-results.md`, and the sprint key is `3-8-decision-3-6-smoke-test-execution-decision-rule-lock-in`. The story, its spec, and its code no longer share a name.
- **PD-2 (accept).** Decision record (a) moved prompts to files in two tiers. `Sources/Summarize/Prompts/` is bundled (`Package.swift` `resources: [.copy("Prompts")]`). The user-override tier `~/.auricle/prompts/` is reachable only through a `promptDir` argument. No code resolves that path from config, because `Config` does not exist (see SR-2). Recorded so it is not re-flagged before `Config` lands.

### Process lessons

- **PL-1 (major, prevention).** The only live test of Citations ran after the Citations code was built.
  - Story 3.5 named "live-API validation of the Citations mechanics remains unverified" as its residual risk and pointed at Story 3.8 (`spec-3-5-…md:200`). Story 3.8's goal says the comparison runs "as the FIRST hour" (`epics.md:1612`), while `epic-3-context.md` sequenced it after 3.5. The two documents disagree.
  - The comparison then found Citations returned no items on 5 of 6 transcripts (`smoke-test-results.md`, Outcome). The decision record's Decision 3.2 already recorded the failure mode as [P12].
  - Cost: `ClaudeCitationsSummarizer.swift` (333 lines), `ClaudeCitationsSummarizerTests.swift` (529 lines), the citations validator, and the fallback path in Story 3.6. This work is not wasted in full. The AC keeps Citations shipped, and the fallback tests still exercise the orchestrator.
  - Prevention: when a story is a go/no-go for another, gate the dependent stories on a cheap live probe, and reconcile conflicting sequencing text in the epic before the first story starts.
- **PL-2 (major, prevention).** `sprint-status.yaml` stopped tracking. Its `last_updated` is 2026-09-16, before the first Epic 3 story. It lists all 12 Epic 3 stories as `backlog`, Stories 1.1 to 1.3 as `review`, and Stories 1.4 to 1.8 and all of Epic 2 as `backlog`, while every one of those specs is `done`. As a result `detect-epic` cannot see finished work, and this retro had to override the gate. `AGENTS.md` already records the pitfall. The tracker was repaired on 2026-09-18 (25 stories and epics 1 to 3 set to `done`; 36 keys renamed by the script's slug cap). The cause is the build flow, not a repo edit. Specs for Stories 1.4 to 3.12 carry `baseline_revision`, the key `bmad-build-auto` writes (`.claude/skills/bmad-build-auto/step-03-implement.md:20`), and that skill has no sprint-status step. Stories 1.2 and 1.3 carry `baseline_commit` and `route: dispatch`, the shape of interactive `bmad-build`. That skill syncs `in-progress` and `review` but never `done` (`step-03-implement.md:27`, `step-05-present.md:17`). The last sync commit (`cdfeabd`, 09-16 12:31) precedes `AGENTS.md` (`15ec345`, 09-16 16:49). Attributing specs to a skill rests on frontmatter shape, not on a session log.
- **PL-3 (medium, prevention).** The `deferred-work.md` ledger fell behind the specs. Specs 3.7 (both), and 3.8 have their deferred entries in the ledger. Specs 3.1, 3.5, 3.9, 3.10 and 3.12 each carry one `deferred` entry in frontmatter that the ledger lacks. The unrecorded 3.12 entry is the medium-severity glossary-dispatch gap (SR-2). Conversely, the ledger has no closure mechanism: the Story 3.7 entry "Record `summarization_prompt_set_hash` in telemetry" was fixed (`Sources/Summarize/SummarizeStage.swift:205`), yet it still reads as open. The 3.8 entries about the human run read as open although `smoke-test-results.md` supersedes them.
- **PL-4 (minor, prevention).** Epic 1 leaked into Epic 3. Commit `6d284d4` added `CacheArtifactWriter`, "the missing Story 1.2 cache-dir writer" (`spec-cache-artifact-writer.md:18`). Story 1.2 logged `CanonicalTranscript` and `Config` as deferred but never logged this sixth primitive (`spec-cache-artifact-writer.md:55`). `CanonicalTranscript` landed with Story 3.1. `Config` is still missing (SR-2).
- Not analyzed: session logs. Why a session took an unexpected turn is unknown except where a spec's review record states it.

## Behavior verification

- **Exercised:** `swift build` then `swift test --explicit-target-dependency-import-check error` on the epic's final commit (`bbad063`). Exit 0. Swift Testing printed "Test run with 675 tests in 5 suites passed". The XCTest run executed 0 tests.
- **Observed in recorded evidence, not re-run:** `Tests/fixtures/smoke-test-results.md` records a live run on 2026-09-18 against `claude-opus-5`. Substring returned items on 6 of 6 transcripts. Citations returned none on 5 of 6.
- **Not exercised:** `auricle-cli __internal-stage summarize` against a real meeting row and a real API call. The `spec-3-7-…md` records say this run was never done. The Google OAuth flow and event matching against real Google. The glossary through dispatch. `make check` (swiftformat, swiftlint, both Xcode schemes). Passing tests do not cover these.

## Previous-retro follow-through

Nothing to follow through on. No `epic-*-retro-*.md` exists, and `sprint-status.yaml` is readable but has no `action_items` key. This is the first retrospective.

## Action items

Owner for every item: the maintainer. Remediation items are proposed for the normal build loop. Spec reconciliations are proposed for a `bmad-correct-course` run. Nothing here was applied.

**Before Epic 4's exit criteria run (they validate the wedge on real audio):**

1. **Build `Core/Config` and pass the vault path through dispatch.** Add a config layer that supplies `vault_path`, and pass `--vault-path` from `SubprocessDispatcher.makeProcess` (`Sources/Orchestrator/SubprocessDispatcher.swift:38`). Without it every dispatched run has an empty glossary and the wedge reads 0 (SR-2). Spec-shaped: one story.
2. **Wire the calendar source, after fixing the matcher.** Construct `GoogleCalendarSource` in `InternalStageWorker` and pass it as `calendarSource:` (SR-3). First fix DR-4: lead-in tolerance and a `fields` mask with `responseStatus`, `eventType` and `transparency`. Needs OAuth client settings, so it depends on item 1.
3. **Make the eval harness test the shipped composition.** Change `EvalDefaultWiring` to substring-only, or move the wiring into `Sources/` so the harness and `InternalStageWorker` share one definition (VG-1).
4. **Fix the speaker-prefix convention.** Decide in Story 4.1's ACs whether utterance ranges include `Speaker_N: `. Strip or align in `SummaryArtifactMapper.transcriptSegments` so notes stop printing the label twice (DR-1).

**Next:**

5. **Give `effortLevel` a job or remove it.** Map it to the API, or delete the field and the two telemetry writes. Decide with item 6 (DR-2, NF-1).
6. **Decide what NFR-C1 measures, then enforce it.** Per meeting or in aggregate. Record the answer for the recorded $0.5682 call in `smoke-test-results.md`, and add a cost check to the summarize stage if the ceiling is per meeting (NF-1).
7. **Classify failures.** Make the item quote optional and count a missing quote as a drop, read `stopReason` and map `max_tokens` to its own class, and map `KeychainError.notFound` to an actionable class (DR-3).
8. **Reconcile the plan to the as-built default (proposed, not applied).** Update `prd.md:515` and `:727`, `architecture.md:1360`, `:2683`, `:2824`, `:2592`, `:2598` (SR-1, AD-1), `epics.md:276`, `:629`, `:1498`, `:1546-1571`, and the stale Story 1.4 lines named in `deferred-work.md`. Decide AD-1 first: move `SummarizationPromptBuilder` out of `Summarize`, or accept the edge and update the graph.
9. **Repair the trackers.** Run `bmad-sprint-planning` to repair `sprint-status.yaml` (PL-2). Append the five spec-only deferred entries to `deferred-work.md` (Stories 3.1, 3.5, 3.9, 3.10, 3.12) and agree a way to mark closed entries (PL-3). Rename the Story 3.8 spec and results file to match the code (PD-1).
10. **Set the file mode on comparison reports** to 0600 (DR-7).

**Decisions only the maintainer can make** (no owner action until answered): keep or replace the two movie-scene fixtures (SR-7); spot-check the recall and false-keep scores in `smoke-test-results.md` (SR-6).

**Process lessons to carry forward:**

- A story that is a go/no-go for other stories gates them on a cheap live probe, and conflicting sequencing text in the epic is reconciled before story one (PL-1).
- A spec that leaves a field "unused" or a wiring step "to the next story" names the owning story, and each side of a two-story hand-off checks the other side's text (DR-2, SR-3).
- `bmad-build-auto` does not sync `sprint-status.yaml`. Repair the file after unattended runs, or a retro cannot read the epic (PL-2).

## Acceptance verdict

**accepted-with-open-items.** Criteria: **declared** (`epics.md:627-641` and `epics.md:1336`).

Evidence for the declared criteria:

- **Standalone value met.** A synthetic transcript plus glossary and calendar input produces a validated `summary.json`, and Persist turns it into a vault note. The cross-stage test and the eval harness cover this, and all 675 tests pass.
- **Strategy family, orchestrator, prompt builder with snapshots, canonicalization tests, prompt caching:** present and tested (`CanonicalTranscriptContractTests.swift`, `cache_control` in `ClaudeSubstringSummarizer.swift`).
- **Decision 3.6 comparison:** run live, outcome and rationale recorded, default locked (`smoke-test-results.md`). The set is public fixtures by maintainer choice (SR-4).
- **Calendar enrichment and vault glossary "ship here as inputs":** built and unit-tested. Neither is live in the production path (SR-2, SR-3). This is the main reason the verdict carries open items.
- **Eval harness:** exists and runs, but it tests a composition that no longer ships (VG-1).
- **NFR-C1 default tier:** assigned to this epic and not evaluated (NF-1).

No finding shows the epic's standalone-value criterion failing, so the retro does not render **rejected** on the findings. Items 1 to 4 gate Epic 4's exit criteria, because Epic 4 sets out to validate the AI-correction wedge on real audio, and that measurement reads 0 until items 1 and 2 land.

**Gate reconciliation.** The unmodified gate is `detect-epic --epic 3`, which lists all 12 stories as pending because `sprint-status.yaml` is stale (`last_updated` 2026-09-16). Under the skill's rule 3 that forces a machine verdict of **rejected**. The retro overrides it on one basis: all 12 Epic 3 specs carry `status: 'done'`, and `AGENTS.md` names each spec's `status` as ground truth over `sprint-status.yaml`. This was a decision made on the maintainer's behalf. It is now moot: the tracker was repaired after this report was first written (25 stories and epics 1 to 3 set to `done`), and the unmodified gate returns `pending_stories: []` for Epic 3. The verdict stands under the gate as written.

## Open questions

1. Resolved: the tracker was repaired and the gate passes without an override.
2. Is NFR-C1's $0.50 ceiling per meeting or in aggregate? The answer decides whether `ami-es2002b` at $0.5682 is a breach.
3. Do the two movie-scene fixtures stay in a public repository?
4. Should the calendar wiring (item 2) land before Epic 4, or wait for Story 9.1's Settings work? It depends on the config layer either way.
5. Does the real transcribe stage's utterance range include the `Speaker_N: ` prefix? Story 4.1 answers this.
