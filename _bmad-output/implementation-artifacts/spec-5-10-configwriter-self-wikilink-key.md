---
title: 'ConfigWriter + `self.wikilink` Key'
type: 'feature'
created: '2026-09-22'
status: 'done'
baseline_revision: '07d3c28cc0ff71d1551cdc30becb2540d9c189b0'
review_loop_iteration: 1
followup_review_recommended: true
context: []
warnings: ['oversized']
deferred:
  - summary: >-
      ConfigVerb.Set.run()'s do/catch to writeStderr/ExitCode wiring has no
      automated test.
    evidence: |-
      Verified: App/-only logic isn't reachable by `swift test` (a
      documented, repo-wide AGENTS.md pitfall — Package.swift's test targets
      only cover Sources/*), and no test anywhere references `ConfigVerb`.
      Raised independently by Blind Hunter, Verification Gap, and the Intent
      Alignment Auditor across both review passes. Not specific to this
      diff: every other CLI verb with comparable do/catch wiring (e.g.
      ImportVerb) is equally untested, so this is a pre-existing, accepted
      repo-wide limitation, not a defect this story introduced.
    location: >-
      App/auricle-cli/Verbs/ConfigVerb.swift (Set.run())
    severity: low
---

<intent-contract>

## Intent

**Problem:** Nothing edits `~/.auricle/config.toml` in place (FR59). `Config` has no `self.wikilink` key, `auricle config set` is a stub, and Story 5.7/5.9's onboarding writes (vault path, `self.wikilink`) have no target.

**Approach:** Add `Core/ConfigWriter.set(_:to:)`, a single-key TOML editor that edits the raw file text (not a parse-and-reserialize round trip) so it never disturbs comments or unrelated keys, and writes through `AtomicWriter`. Extend `Config`/`RawConfig` with `self.wikilink` → `selfWikilink: String?`. Wire `auricle config set <key> <value>` to call it.

## Boundaries & Constraints

**Always:**
- `ConfigWriter.set(_ key: String, to value: String, fileURL: URL? = nil, homeDirectory: URL = FileManager.default.homeDirectoryForCurrentUser) throws` — `fileURL`/`homeDirectory` mirror `Config.load`'s own test-seam parameters (`Config.defaultFileURL(homeDirectory:)` is the resolution default), so tests never touch the real `~/.auricle`.
- `key` is dotted (`"self.wikilink"`) or bare (`"vault_path"`) — the same two shapes `Config`'s own schema already uses. Support both an existing fully-dotted line (`table.key = ...`) and an existing `[table]` section form as the pre-existing representation to edit in place; when creating a new nested key, always emit the `[table]` section form.
- Changes only the target key's value. Every other key, table, comment, and line of the file is byte-for-byte unchanged, including a trailing inline comment (`# ...`) on the edited line itself, which survives after the new value.
- Creates `~/.auricle/` and `config.toml` when either is absent, writing a file containing only the new key.
- The final write goes through `Core/AtomicWriter.write(_:to:)` — never a direct `Data.write`/`FileManager` write (the existing `atomic_writer_bypass` swiftlint rule already covers `Sources/Core/*.swift`).
- `Config.parse` gains a `self` table: `selfWikilink: String?`, empty-string-means-unset like every other optional string key (`nonEmpty(_:)`, already in `Config.swift`).
- `ConfigVerb.Set.run()` calls `ConfigWriter.set(arguments.key, to: arguments.value)`, replacing its `notYetImplemented` stub, and reports a typed failure the same way `ImportVerb` does (`writeStderr` + non-zero `ExitCode`, not a crash).

**Never:**
- Never parse the whole document into a `TOMLTable` and reserialize it to satisfy "keeps every comment" — verified in `TOMLKit`'s vendored `toml.hpp` (`consume_comment()`, called from the table/array-of-tables parse loops) that the parser discards comment text as it consumes it; nothing attaches a comment to a node, so `TOMLTable(string:).convert(to: .toml)` reproduces values only, never the original comments. Editing must operate on the raw file text.
- Never accept or write a secret through this path — `ConfigWriter` has no parameter for one, and the Anthropic API key and Google refresh token stay in `KeychainAPIKey`/Keychain (NFR-S1); they are not part of `Config`'s TOML schema and this story adds no schema entry for either.
- Never touch `ConfigVerb.Get` — it stays `notYetImplemented`; this story only wires `Set`.
- `key` paths deeper than one dot (e.g. three-level nesting) are out of scope — no key in `Config`'s current schema needs it.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Existing top-level key | File has `vault_path = "/old"` plus unrelated comments/keys | Only that line's value changes to the new literal; every other line is untouched | No error expected |
| Existing nested key, `[table]` form | File has `[self]\nwikilink = "[[Old]]"` with a comment above it | Only `wikilink`'s value changes; the section header, comment, and any sibling keys in `[self]` are untouched | No error expected |
| Existing nested key, fully-dotted form | File has `self.wikilink = "[[Old]]"` at top level (no `[self]` header) | That line's value changes in place; no duplicate `[self]` section is created | No error expected |
| Key absent, table absent | File has no `[self]` section at all | A new `[self]\nwikilink = "..."` section is appended at end of file | No error expected |
| Key absent, table exists | File has `[self]` with unrelated keys, no `wikilink` | `wikilink = "..."` is appended inside the existing `[self]` section, before the next table header (or EOF) | No error expected |
| File absent | `~/.auricle/config.toml` doesn't exist (and `~/.auricle/` may not either) | Both are created; the file contains only the new key | No error expected |
| Trailing inline comment on the edited line | `wikilink = "[[Old]]" # picked at onboarding` | New value replaces `"[[Old]]"`; ` # picked at onboarding` is preserved unchanged after it | No error expected |
| Value needs TOML escaping | Value contains a `"` and a `\` | Written as a valid, correctly-escaped TOML basic string; `Config.parse` reads back the original unescaped value | No error expected |
| `self.wikilink` round trip | `ConfigWriter.set("self.wikilink", to: "[[Andrew]]")`, then `Config.parse` on the result | `config.selfWikilink == "[[Andrew]]"` | No error expected |

</intent-contract>

## Code Map

- `Sources/Core/ConfigWriter.swift` -- new. `public enum ConfigWriter { public static func set(_ key: String, to value: String, fileURL: URL?, homeDirectory: URL) throws }`. Sibling to `Config.swift` in the same target/directory. `WriterError` gains an `invalidKey(key: String)` case alongside the existing `malformed` (review iteration 1, see Spec Change Log) — thrown by the new key-shape validation in Design Notes.
- `Sources/Core/Config.swift:26-88` -- `Config` struct + `init`; add `selfWikilink: String?` alongside the existing optional fields, following the `vaultPath`/`nonEmpty` pattern already used for `googleCalendar`.
- `Sources/Core/Config.swift:120-146` -- `Config.parse`; add `selfWikilink: nonEmpty(raw.self?.wikilink)` to the returned `Config(...)`.
- `Sources/Core/Config.swift:199-236` -- `RawGoogleCalendar`/`RawConfig` — add a `RawSelfTable { let wikilink: String? }` (case name can't be `self`; use e.g. `case selfTable = "self"` in `RawConfig.CodingKeys`) and a `selfTable: RawSelfTable?` field on `RawConfig`.
- `Sources/Core/Config.swift:89-93` -- `Config.defaultFileURL(homeDirectory:)` — `ConfigWriter` resolves its target path the same way, so a caller passing no `fileURL` writes exactly where `Config.load` reads.
- `Sources/Core/AtomicWriter.swift:35` -- `AtomicWriter.write(_:to:permissions:)` — `ConfigWriter`'s sole write call.
- `App/auricle-cli/Verbs/ConfigVerb.swift:27-38` -- `ConfigVerb.Set.run()`; replace `try notYetImplemented("config set")` with the `ConfigWriter.set` call, following `App/auricle-cli/Verbs/ImportVerb.swift:22-57`'s `do`/`catch` → `writeStderr` + `ExitCode(1)` shape (no `StateStore` needed here, so no `openStateStore` equivalent).
- `Sources/Orchestrator/CLIVerbArguments.swift:194-201` -- `ConfigSetArguments` (`key: String`, `value: String`) already exists; no change needed.
- `Tests/CoreTests/ConfigTests.swift:1-19` -- existing `makeFakeHome`/`writeConfig` test helpers; `ConfigWriterTests.swift` follows the same fake-home pattern.
- `Tests/CoreTests/ConfigTests.swift:28-` -- `parsesEveryKnownKey`; add a `[self]\nwikilink = "..."` case and assert `selfWikilink`.
- `.../SourcePackages/checkouts/TOMLKit/Sources/CTOML/Sources/toml.hpp:13687` (vendored dependency, not part of this repo) -- `consume_comment()`, the evidence that comments are discarded during parsing, cited in Boundaries above.

## Tasks & Acceptance

**Execution:**
- `Sources/Core/ConfigWriter.swift` -- implement `set(_:to:fileURL:homeDirectory:)`: locate the target line via the raw-text search described in Design Notes, replace or append, then `AtomicWriter.write`.
- `Sources/Core/Config.swift` -- add `selfWikilink` end to end (`Config`, `Config.parse`, `RawConfig`/`RawSelfTable`).
- `App/auricle-cli/Verbs/ConfigVerb.swift` -- wire `Set.run()` to `ConfigWriter.set`.
- `Tests/CoreTests/ConfigWriterTests.swift` -- new. One `@Test` per I/O-matrix row above, plus one per invalid-key case in Design Notes (review iteration 1).
- `Tests/CoreTests/ConfigTests.swift` -- extend `parsesEveryKnownKey` (or add a sibling test) for `self.wikilink`.

**Acceptance Criteria:**
- Given a config file with unrelated keys and comments, when `ConfigWriter.set` changes one key, then every other line in the file is unchanged and the target key's new value round-trips through `Config.parse`.
- Given no `~/.auricle/config.toml`, when `ConfigWriter.set` is called, then the directory and file are created and contain only the new key.
- Given `auricle config set self.wikilink "[[Andrew]]"`, when the CLI runs, then `ConfigWriter.set` is invoked (not `notYetImplemented`) and a subsequent `Config.load` reports `selfWikilink == "[[Andrew]]"`.
- Given `ConfigWriter.set` fails (e.g. an unwritable `~/.auricle/`), when the CLI verb runs, then it exits non-zero with a message on stderr, not a crash.
- Given `key` is empty, is exactly `"."`, or starts or ends with `.` (e.g. `"self."`, `".wikilink"`), when `ConfigWriter.set` is called, then it throws `WriterError.invalidKey` before reading or writing any file, and no line is inserted or altered. (review iteration 1)

## Spec Change Log

### 2026-09-22 — Review pass 1 (bad_spec)

**Triggering findings:** Blind Hunter and Edge Case Hunter (two independent findings each, same root cause) found that `ConfigWriter.set` never validates `key`'s shape: a trailing dot (e.g. `"self."`) yields an empty `leaf`, so the code silently inserts a keyless `" = value"` line into the user's hand-edited config file with no error; a leading dot (e.g. `".wikilink"`) yields an empty `table`, so the code silently falls into the bare-key branch and writes a root-level `wikilink` key instead of erroring. Separately, Blind Hunter found that an existing table already defined in inline form (`self = { wikilink = "x" }`) isn't recognized by the `[table]`-header search, so `set` appends a second, block-form `self` section rather than editing the inline one in place — a case this spec's Boundaries never named as supported or excluded.

**What was amended:** Design Notes gained a mandatory key-validation step, run before any file I/O, rejecting an empty key or one with a leading/trailing dot via a new `WriterError.invalidKey` case (Code Map and Tasks & Acceptance updated to match: a new AC bullet, a new test-coverage bullet). Design Notes also gained an explicit "out of scope" paragraph stating that only the two representations already named in Boundaries (fully-dotted line, `[table]` section) are recognized — an inline-table definition of the same table is not detected, and `set` will append a redundant block-form section alongside it. Neither change touches `<intent-contract>`: the fix for the first finding is a Design Notes procedural step (validate before acting), and the fix for the second is a Design Notes scope clarification, not a change to Boundaries' own "Always"/"Never" rules.

**Known-bad state avoided:** a user's hand-maintained `~/.auricle/config.toml` — the exact file FR59 promises is safe to edit by hand — could have been silently corrupted with a malformed line (trailing dot) or had a value land under the wrong key (leading dot) with no error surfaced at all. The inline-table gap is now a documented, deliberate limitation instead of a silent one.

**KEEP instructions (must survive re-derivation):** everything about the original approach was independently verified correct except the two gaps above: raw-text editing instead of a `TOMLTable` parse/reserialize round trip (confirmed necessary — `toml++` discards comments while parsing); `TOMLTable(["v": value]).convert(to: .toml, options: [])` for value-literal formatting (confirmed correct, including that `options: []` is required to avoid `.allowLiteralStrings` rendering a safe value as a single-quoted literal); the root-region/`[table]`-section search-and-replace algorithm, including trailing-comment preservation and blank-line-aware insertion; the `Config`/`RawConfig`/`RawSelfTable` additions; the `ConfigVerb.Set.run()` wiring shape (`do`/`catch` → `writeStderr` + `ExitCode(1)`, matching `ImportVerb`). Re-derive by keeping all of that and adding only the key-shape validation and the inline-table scope note.

## Review Triage Log

### 2026-09-22 — Review pass 1
- verdicts: 23 findings — high 0, medium 5, low 10, false 8, maybe-false 0
- findings:
  - `[medium]` `[bad_spec]` (Blind Hunter) A trailing or malformed dot in `key` (e.g. `"self."`) yields an empty `leaf`, silently writing a keyless `" = value"` line into the user's config file instead of erroring. Verified by tracing `apply(key:literal:to:)`: `lineDefines(_, key: "")` matches any line starting with `=`, so the insertion path fires with an empty `leaf`. Amended: mandatory key validation (see Spec Change Log).
  - `[medium]` `[bad_spec]` (Edge Case Hunter, same root cause) A leading dot (e.g. `".wikilink"`) makes `table` empty, silently falling into the bare-key branch and writing a root-level key instead of erroring. Verified by tracing the same function: `dotIndex` at position 0 makes `table == ""`. Grouped with the above.
  - `[medium]` `[bad_spec]` (Edge Case Hunter, same root cause) A bare `"."` or an empty `key` produces the same class of silent malformed write. Grouped with the above.
  - `[medium]` `[patch]` (Edge Case Hunter) `isHeader(_:named:)` requires an exact `"[name]"` match after trimming, so a `[self]` header carrying a trailing comment (`"[self] # note"`) or non-canonical spacing (`"[ self ]"`) isn't recognized as the existing table — `set` appends a duplicate `[self]` section instead of editing in place. Verified by reading `isHeader`'s exact-equality check. Real and plausible for a hand-edited file (FR59's whole premise), but the fix (loosen `isHeader`'s comparison) touches only a private function — no new public surface — so it's a clean patch, not a spec change. Moot this pass: `bad_spec` findings above force a full revert-and-re-derive, so patching the soon-to-be-discarded code now would be wasted; this will be re-found and patched against the re-derived code next pass.
  - `[medium]` `[patch]` (Edge Case Hunter, same root cause, filed as a claim against the AC) Same underlying gap restated against Boundaries' own "keeps every comment"/"every other line unchanged" claims — a header-line comment breaks that guarantee. Grouped with the above; moot this pass for the same reason.
  - `[low]` `[defer]` (Blind Hunter) An existing table defined only in inline form (`self = { wikilink = "x" }`) isn't recognized, so `set` appends a redundant block-form section rather than editing the inline one. Verified by reading `isHeader`'s and the section-search logic, neither of which parses inline-table syntax. Real but narrow: no caller in this codebase (CLI or the not-yet-built onboarding writers) ever produces inline-table output, and correctly rewriting an inline table's contents in place is a meaningfully bigger feature than a single-key text edit. Amended: Design Notes now names this a deliberate, documented scope limit (see Spec Change Log) rather than leaving it an unstated gap — resolved via `bad_spec`'s spec-clarification path, no code fix required.
  - `[false]` `[reject]` (Blind Hunter) `isTopLevelHeader` doesn't distinguish a `[[table]]` array-of-tables header from a plain `[table]` header. Verified by reasoning through TOML semantics and this codebase's schema: an array-of-tables header genuinely does bound the previous table's extent in real TOML, so treating it as a section boundary is correct, not a defect; `Config`'s schema also defines zero array-of-tables fields, so no real key ever exercises this path.
  - `[low]` `[patch]` (Blind Hunter) `WriterError.malformed` (thrown when the existing file isn't valid UTF-8) has zero test coverage. Verified: `ConfigWriterTests.swift` covers every I/O-matrix row but not this error path, and it's straightforwardly testable (write invalid-UTF8 bytes directly, bypassing `AtomicWriter`). Moot this pass (see above); to be added as a test on the next pass.
  - `[low]` `[patch]` (Blind Hunter, two findings, same location) `Config.swift`'s new `selfWikilink` doc comment asserts a behavior nothing in this diff implements or consumes ("Unset means no self attribution is applied" — no code yet reads `Config.selfWikilink` outside its own parser/tests) and separately cites story numbers ("Story 5.7/5.9's onboarding writes this") inside shipped source, which this project's own comment conventions treat as process residue that rots as story numbering evolves. Verified by reading the comment and confirming (via the Verification Gap reviewer's own search) nothing consumes the field yet. Moot this pass; to be reworded on the next pass to describe only the field's shape, not unimplemented behavior or story numbers.
  - `[false]` `[reject]` (Blind Hunter) `Set.run()` produces no output on success, unlike `ImportVerb`. Refuted: `ImportVerb` prints because it must hand the caller a generated meeting ID it has no other way to learn; `config set` has no such value to report, and silence-on-success is a standard, arguably preferable CLI convention (e.g. `git config set`) — no demonstrated harm.
  - `[false]` `[reject]` (Blind Hunter) `replacingValue`'s quoted-string scanner only special-cases 2-character escapes (`\"`, `\\`, …), so a `\uXXXX`/`\UXXXXXXXX` unicode escape in an existing value might be mis-scanned. Refuted by tracing the scanner: after skipping the 2-character `\u`/`\U` marker, the remaining hex digits are plain characters that can't equal the quote or trigger another escape, so they pass through harmlessly and the real closing quote is still found correctly — no realistic input reaches the claimed failure.
  - `[low]` `[defer]` (Blind Hunter, Verification Gap, Intent Alignment Auditor — same finding, three sources) The `ConfigVerb.Set.run()` do/catch → `writeStderr`/`ExitCode` wiring has no test — `App/`-only logic isn't reachable by `swift test` (a documented, repo-wide `AGENTS.md` pitfall), and no test anywhere references `ConfigVerb`. Verification Gap's own filed disposition (trusted per this pass's rules): this is the same pattern already accepted for every other verb (e.g. `ImportVerb`'s comparable wiring is equally untested) — a repo-wide decision, not specific to this diff.
  - `[low]` `[patch]` (Edge Case Hunter) `appendingSection` can add one extra blank line when the file already ends in a blank line before a brand-new top-level table is appended (the section-insert path already trims trailing blanks; this path doesn't). Verified by tracing `apply`'s line-splitting: a trailing-blank-line file yields a trailing empty-string element that `appendingSection` doesn't strip. Cosmetic only, fix is a trivial mirror of the existing trim logic. Moot this pass. **[Correction, pass 2]** the re-derivation (unprompted) already trims trailing blanks in `appendingSection` — this row's "moot, to be added next pass" framing is stale; no further action needed. Bookkeeping fix only, per this spec's own pass-3-of-`VaultWriter`-style precedent for correcting a stale log line in place; not re-counted as a pass-2 finding.
  - `[false]` `[reject]` (Intent Alignment Auditor) The diff's own new spec file frontmatter reads `status: 'in-review'`, not `'done'`, in tension with "complete story 5.10." Not a defect: this is the expected, transient workflow state at the point the diff was captured (before this step's own Finalize sets `status: done`).
  - `[low]` `[defer]` (Intent Alignment Auditor) Same untested-CLI-wiring finding as above, restated from the intent-alignment angle (the AC's literal subject is the CLI invocation, verified only by a prescribed manual step). Not double-counted as a separate defect; same disposition.
  - `[false]` `[reject]` (Intent Alignment Auditor) "Readiness gate for phase two is unconfirmed" — the auditor had visibility only into the diff file, not this session's own verification. Refuted directly: `swift build`, `swift test --filter CoreTests` (159/159 passed), `swiftformat --lint .` and `swiftlint --strict` (0 violations), and `xcodebuild -scheme auricle-cli build` (BUILD SUCCEEDED) were all run against this exact diff after it was captured, before this review pass began.
  - `[false]` `[reject]` (Intent Alignment Auditor) "Phase two (ship) hasn't started" — accurate but not a defect; it's the correct, expected sequencing of this workflow (implement → review → ship), not evidence of a problem.
  - `[low]` `[defer]` (Intent Alignment Auditor) The active branch `claude/bmad-build-autocomplete-5-10-6a79b6` carries the auto-generated `claude/` prefix `AGENTS.md`'s own Policy section forbids for branch names. Real, but not caused by this story's code — the branch was assigned by the harness before any of this story's changes — so it's a pre-existing condition, not a `ConfigWriter` defect. Handled operationally at the `/quality:ship` stage (a semantically-named branch will be created before opening the PR), not via a code or spec change here.
  - `[false]` `[reject]` (Verification Gap Reviewer, "Other findings") `Config.swift`'s new `selfWikilink` and the pre-existing, structurally-similar calendar-derived self-identity field (`CalendarEnrichment.Match` → ... → `FilenameResolver`) are two unrelated sources today. The reviewer's own framing states this explicitly: not a missing-adoption gap, since this story's own spec frames the new field as scaffolding for a not-yet-built onboarding story — no demonstrated bad outcome yet.
  - `[false]` `[reject]` (Edge Case Hunter) `formattedLiteral` assumes `TOMLTable(["v": value]).convert(to: .toml, options: [])` always renders with a literal `"v = "` prefix, guarding against it not doing so. Refuted: with `options: []` (disabling `.allowLiteralStrings`), a String value under a valid bare key named `"v"` is always rendered as a double-quoted basic string with that exact prefix — no value ConfigWriter ever puts in that table can produce a different rendering, so the claimed failure has no reachable trigger.

### 2026-09-22 — Review pass 2
- verdicts: 18 findings — high 0, medium 4, low 6, false 8, maybe-false 0
- findings:
  - `[low]` `[reject]` (Blind Hunter) A hand-written multi-line (triple-quoted) string value whose body contains a line starting with `[` is misread by the line-scanner as a table header, which can narrow a search range enough to insert a new key inside the string body. Verified by tracing `isTopLevelHeaderLine`/section-boundary logic against a constructed example. Real, but rejected: no key in `Config`'s schema is ever written as a multi-line string, this file's realistic content (paths, IDs, model names, wikilinks) never plausibly contains an embedded `[`-prefixed line, and a correct fix needs full multi-line-string-aware tokenization — a materially bigger change than a single-key text edit, for a scenario unlikely to occur in practice.
  - `[medium]` `[patch]` (Blind Hunter) `key` validation only rejects empty/leading-dot/trailing-dot; it doesn't reject characters illegal in a bare TOML key (spaces, `[`, `]`, `#`, `=`, …), so e.g. `auricle config set "a b" x` writes syntactically invalid TOML with no error, which then makes the *entire* file fail to parse on next `Config.load`. Verified by tracing `splitKey`/the bare-key insertion path: nothing rejects a table/leaf component containing such characters. Fix: widen the existing key-shape guard to a bare-TOML-key character-class check, reusing the same `WriterError.invalidKey` case added in pass 1 — no new public surface.
  - `[false]` `[reject]` (Blind Hunter) `set` never validates that `key` names a field `Config.parse` actually recognizes, so a typo'd key succeeds silently. Refuted: `Config.swift`'s own doc comment already establishes the opposite convention deliberately — "Keys the file carries that this type does not know are ignored, so a newer file still loads" — `ConfigWriter` writing a key it (and `Config`) doesn't recognize is consistent with that existing, intentional forward-compatibility design, not a defect.
  - `[false]` `[reject]` (Blind Hunter) The read-modify-write in `set` has no protection against two concurrent writers. Refuted: `AtomicWriter.write`'s own doc comment already states plainly that it is "not safe to call concurrently for the same path" and that callers must serialize their own writes — this is `AtomicWriter`'s pre-existing, already-documented, already-accepted contract, not something this story's composition of it newly introduces.
  - `[false]` `[reject]` (Blind Hunter) The new `selfWikilink` doc comment describes the expected `[[Name]]` shape by example, but nothing validates or normalizes a value against it. Refuted: no AC or FR requires wikilink-shape validation, and it matches this codebase's existing "trust the caller/config" boundary for free-text fields (e.g. `meetingsSubdir`'s shape is checked only for filesystem safety, not semantically).
  - `[low]` `[patch]` (Blind Hunter) `ConfigVerb.Set.run()`'s generic catch-all still reports only `type(of: error)` for an error that isn't `ConfigWriter.WriterError` (the typed cases already get a real message via pass 1's `.message` addition). Verified by reading the catch chain. Fix: use `String(describing: error)` instead of `type(of: error))` in the fallback branch — trivial, no new public surface.
  - `[low]` `[reject]` (Blind Hunter) Pass 1's Review Triage Log describes `appendingSection`'s blank-line handling as still needing a fix "next pass," but the re-derived code already contains it. Real staleness, but its fix is to edit this build's own spec log — explicitly excluded from patch/bad_spec routing. Corrected directly in place above (bookkeeping only, not a counted finding), matching this project's own precedent for this exact class of fix.
  - `[false]` `[reject]` (Blind Hunter) Boundaries' "key paths deeper than one dot are out of scope" isn't enforced in code, so a 3-component key like `"a.b.c"` silently produces `table = "a.b"`, `leaf = "c"`. Refuted: `[a.b]` is exactly TOML's own standard notation for a two-level nested table, so this split is the semantically correct representation of a 3-component dotted path, not a corruption — "out of scope" describes `Config`'s current *schema* (nothing reads a 3-level field today), not a code path that needs rejecting.
  - `[low]` `[reject]` (Edge Case Hunter) Calling `set` with a bare key matching an existing table's name (e.g. `set("self", …)` when `[self]` already exists), or a dotted key when the same name is already a bare scalar, can produce a document that defines the same name as both a table and a scalar — invalid TOML. Verified by tracing both directions. Real but narrow: no real caller (CLI usage matching a documented key, or the onboarding writers) ever calls `set` with a bare name that collides with an existing table's own name, and a general fix needs whole-document name/kind-conflict detection, a materially larger change than this story's single-key edit.
  - `[medium]` `[patch]` (Edge Case Hunter) `replacingValue`'s bare-token scanner (for a non-quoted existing value) stops at the first whitespace, so replacing a key whose existing value is an array or inline table (e.g. `key = [1, 2, 3]`) truncates it mid-token, producing invalid TOML (e.g. `key = "new" 2, 3]`). Verified by tracing `endOfValueToken`'s `default` case. Real and more plausible than the multi-line-string case above, since `config set <key> <value>` is a general-purpose command a user could point at any existing key, including one they gave a non-scalar value. Fix: track `[`/`{` nesting depth in the bare-token scan so embedded whitespace inside a still-open array/inline-table doesn't end the token early — bounded, no new public surface.
  - `[false]` `[reject]` (Edge Case Hunter, same finding as Blind Hunter's concurrency finding above) Two concurrent `config set` invocations can race. Not double-counted; same refutation (`AtomicWriter`'s own pre-existing, documented contract).
  - `[medium]` `[patch]` (Edge Case Hunter) `firstHeaderIndex`'s exact `"[name]"` match (after trimming) doesn't recognize a `[table]` header carrying a trailing comment (`"[self] # note"`) or non-canonical internal spacing (`"[ self ]"`) as the existing table, so `set` appends a duplicate section instead of editing in place — breaking Boundaries' own "every other line unchanged"/"keeps every comment" guarantee for a plausible hand-edited-file style. First surfaced in pass 1 (moot then, since a `bad_spec` finding forced a full revert); no `bad_spec`/`intent_gap` this pass, so now actionable. Fix: loosen the header match to tolerate a trailing `#...` comment and interior whitespace — no new public surface.
  - `[medium]` `[patch]` (Edge Case Hunter, same root cause, filed as a claim against the Acceptance Criteria) Same header-tolerance gap restated against the "target key's new value round-trips through `Config.parse`" AC — a duplicated `[self]` section can make `Config.parse` see the stale value or fail outright. Grouped with the above; same fix.
  - `[false]` `[reject]` (Verification Gap Reviewer) No verification gaps found this pass.
  - `[low]` `[patch]` (Verification Gap Reviewer, "Other findings") `Tests/CoreTests/ConfigTests.swift` and `ConfigWriterTests.swift` hard-code the real name `"[[Andrew]]"` as sample `self.wikilink` data in several places, where the rest of this codebase's test fixtures consistently use placeholder names (`[[Ben]]`, `[[Jordan]]`, `[[Ada Lovelace]]`) and this story's own spec verification command already uses `'[[Test]]'`. Verified by reading both test files. This also engages the maintainer's own standing instruction (never his real name in anything that could become public, code included) — not just a style nit. Fix: rename to a placeholder wikilink matching existing test-fixture convention, no behavior change.
  - `[false]` `[reject]` (Intent Alignment Auditor) The ship clause (`/quality:ship`) isn't exercised by this diff — no commit, no PR. Not a defect: correct, expected sequencing (implement → review → ship), reaffirming pass 1's identical disposition.
  - `[false]` `[reject]` (Intent Alignment Auditor) The spec's own `status` isn't `done` yet, so the readiness bar this repo uses (frontmatter `status`, per `AGENTS.md`) hasn't been crossed. Not a defect: expected, transient state before this step's own Finalize sets `status: done`; reaffirms pass 1.
  - `[low]` `[defer]` (Intent Alignment Auditor) The CLI-invocation surface the AC names is verified only by a manual step, not a test, restating the App/-untested-wiring gap. Not double-counted; same disposition as Blind Hunter's/Verification Gap's identical finding, now recorded in frontmatter `deferred` above.
  - `[false]` `[reject]` (Intent Alignment Auditor) `Config.selfWikilink` exists only at the schema/parse surface; nothing yet consumes it (e.g. an attribution feature). Reaffirms pass 1: the spec explicitly frames this as scaffolding for a not-yet-built onboarding story, not a defect.

## Design Notes

**Why raw-text editing instead of `TOMLTable` parse/reserialize:** `TOMLKit`'s backend (`toml++`) drops comment tokens as it parses (`consume_comment()` in the vendored `toml.hpp`) — nothing in its node model stores them. Round-tripping the whole file through `TOMLTable(string:)` → mutate → `.convert(to: .toml)` would silently delete every comment, which the AC explicitly forbids. `TOMLKit` is still used, but only to format the one new value correctly (quoting/escaping), not to reserialize the document.

**Validate `key` before touching any file** (added review iteration 1 — see Spec Change Log): split on the last `.`, as below, to get `table` (may be empty) and `leaf`. Throw `WriterError.invalidKey(key: key)` first, before any read or write, when `leaf` is empty (covers `key` being empty, exactly `"."`, or ending in `.`) or when `table` is non-empty but starts with `.` or is itself empty-after-the-dot in a way that isn't the ordinary "no dot at all" case (covers `key` starting with `.`, e.g. `".wikilink"`). Concretely: reject whenever `key` is empty, or `key.hasPrefix(".")`, or `key.hasSuffix(".")`. Without this, a leading dot silently drops into the bare-key branch (writing `wikilink` at the root instead of erroring) and a trailing dot writes a keyless `" = value"` line — both silent corruptions of a file `~/.auricle/config.toml` FR59 promises is safe to hand-edit.

**Explicitly out of scope (review iteration 1):** recognizing an inline-table assignment (`self = { wikilink = "x" }`) as an existing occurrence of table `self`. `set` only ever recognizes the two forms named in Boundaries — a fully-dotted line, or a `[table]` section header. If a table is already defined only in inline form, `set` does not find it and appends a fresh `[table]` section instead, which stands alongside the untouched inline definition. Nothing in this codebase (the CLI, or the onboarding writers Stories 5.7/5.9 will add) ever produces inline-table config output, so this only matters for a config file someone hand-wrote using inline-table style — narrow enough that rewriting an inline table's contents in place (a meaningfully bigger feature than a single-key text edit) isn't worth building for this story.

**Locating and replacing a line, in order:**
1. Split `key` on `.`: `table` = everything but the last component (may be empty), `leaf` = last component.
2. If `table` is non-empty, first look for an existing fully-dotted line `^\s*table\.leaf\s*=` anywhere before the first top-level `[...]` header (the dotted form is only meaningful at the root). If found, replace there.
3. Else if `table` is non-empty, find a `^\[table\]` header line; if present, search within its section (up to the next `^\[` or EOF) for `^\s*leaf\s*=`. Replace if found; otherwise insert `leaf = literal` just before the section ends.
4. Else if `table` is non-empty and no `[table]` section exists, append `\n[table]\nleaf = literal\n` at EOF.
5. Else (`table` empty): search the root region (before the first `^\[` header) for `^\s*leaf\s*=`; replace if found, else insert the new line at the end of that region.
6. **Replacing a found line's value:** everything up to and including `=` and its following whitespace is kept; the existing value token is skipped (a `"..."` or `'...'` span honoring `\"` escapes in the former, or a bare token up to whitespace/`#`/EOL otherwise) and replaced with the new literal; anything after the old value (whitespace + optional `# comment`) is preserved verbatim.
7. **Formatting the literal:** `TOMLTable(["v": value]).convert(to: .toml)`, then take the substring after `"v = "` (trimming the trailing newline) — reuses `TOMLKit`'s own escaping instead of hand-rolling it.

## Verification

**Commands:**
- `swift test --filter CoreTests` -- expected: `ConfigWriterTests` and the extended `ConfigTests` pass.
- `swift build` -- expected: clean build, no new external dependency (TOMLKit is already a `Core` dependency).
- `swiftformat --lint .` and `swiftlint lint --strict --config .swiftlint.yml .` -- expected: 0 violations.
- `cd App && tuist generate --no-open && xcodebuild -workspace App/Auricle.xcworkspace -scheme auricle-cli build` -- expected: clean build; then run the built `auricle-cli config set self.wikilink '[[Test]]'` against a scratch `HOME` and confirm `~/.auricle/config.toml` contains it.

## Auto Run Result

**Summary:** Implemented `Core/ConfigWriter.set(_:to:fileURL:homeDirectory:)`, a single-key TOML editor that rewrites `~/.auricle/config.toml` by operating on raw file text — never a parse/reserialize round trip, since `TOMLKit`'s `toml++` backend discards comments while parsing. Added `Config.selfWikilink: String?` (a new `self` table, `RawSelfTable`), and wired `auricle config set <key> <value>` to `ConfigWriter.set`, replacing its stub. Two review passes ran: pass 1 found a real key-validation gap and forced a `bad_spec` revert-and-re-derive; pass 2 found four further patchable gaps (bare-key character validation, an array/inline-table value-truncation bug, table-header comment/whitespace intolerance, and a real name in test fixtures), all fixed via direct re-engagement of the implementation agent. Both passes' full findings, verdicts, and dispositions are in the Review Triage Log above.

**Files changed:**
- `Sources/Core/ConfigWriter.swift` -- new. `ConfigWriter.set(_:to:fileURL:homeDirectory:)`; `WriterError` (`invalidKey`, `malformed`).
- `Sources/Core/Config.swift` -- added `selfWikilink: String?` end to end (`Config`, `Config.parse`, `RawConfig`/`RawSelfTable`).
- `App/auricle-cli/Verbs/ConfigVerb.swift` -- `Set.run()` now calls `ConfigWriter.set`, mapping failures to a stderr message + `ExitCode(1)`.
- `Tests/CoreTests/ConfigWriterTests.swift` -- new, one test per I/O-matrix row plus invalid-key, malformed-file, array-value, and header-tolerance cases.
- `Tests/CoreTests/ConfigTests.swift` -- extended for `self.wikilink` parsing.

**Review findings breakdown across 2 review passes** (full detail in the Review Triage Log above):
- **Pass 1 → bad_spec (3 findings, 1 root cause; patched: 0, high 0, medium 0):** `ConfigWriter.set` never validated `key`'s shape — a leading or trailing dot silently corrupted the file (a keyless line) or wrote to the wrong key (root-level instead of nested), with no error. Fixed with a mandatory key-validation step and a new `WriterError.invalidKey` case, plus a Design Notes scope note explicitly excluding inline-table recognition (a separate, narrower `bad_spec` finding). Code was reverted and fully re-derived from the amended spec.
- **Pass 2 → patch (4 groups, 5 member findings; patched: high 0, medium 3, low 2):** bare-key character validation was still incomplete (illegal characters like a space were accepted, producing invalid TOML); `replacingValue`'s bare-token scanner truncated an existing array/inline-table value at the first embedded whitespace; `firstHeaderIndex`'s exact string match didn't recognize a `[table]` header carrying a trailing comment or non-canonical spacing, causing a duplicate section; the CLI's generic error path showed only an error's type name; and two test files hard-coded the maintainer's real name as sample data instead of a placeholder. All five were fixed directly (see Review Triage Log for each fix), each covered by a new or extended test, verified independently after the patch.
- **Rejected (13 findings across both passes):** each with its refutation recorded in the Review Triage Log — notably a multi-line-string false-header-detection gap (real but requiring config shapes never used in this codebase and a materially larger fix), a table/scalar namespace-collision gap (real but narrow), and several findings refuted by this codebase's existing, already-documented conventions (`Config`'s forward-compatible unknown-key tolerance, `AtomicWriter`'s documented non-concurrency contract, the "out of scope" boundary for 3-level keys turning out to be harmless because TOML's own nested-table syntax already covers it correctly).
- **Deferred (1 item, in frontmatter `deferred:`):** `ConfigVerb.Set.run()`'s CLI-level wiring has no automated test, since `App/`-only logic isn't reachable by `swift test` — a pre-existing, repo-wide, already-accepted limitation (e.g. `ImportVerb`'s comparable wiring is equally untested), not specific to this story.

**Follow-up review recommendation: `true`.** Pass 2 patched three `medium`-severity findings (the ≥2-medium threshold). Named unverified risk: the three medium patches — bare-key character validation, the bracket/brace-depth-aware value-token scanner, and the header comment/whitespace-tolerant match — are each covered by a new unit test for their originally reported trigger, but haven't been independently adversarially re-reviewed the way the original implementation was. A follow-up pass could probe further edge cases the current tests don't cover (e.g. a nested array `[[1, 2], [3, 4]]`, or a `#` character legitimately inside a quoted table name).

**Verification performed:** `swift build` and `swift test` (full suite) after every implementation/re-derivation/patch cycle — final: 163/163 `CoreTests`, full suite green; `swiftformat --lint .` and `swiftlint lint --strict --config .swiftlint.yml .` clean at every checkpoint (0 violations, 385 files); `xcodebuild -workspace App/Auricle.xcworkspace -scheme auricle-cli build` succeeded at every checkpoint. Every I/O-matrix row plus every new patch-driven test was individually confirmed running and passing (not just a suite-level pass count) before and after each review pass, per the Matrix Test Audit.

**Residual risks:** the named follow-up-review risk above; the accepted, documented inline-table-recognition scope limit (Design Notes); the deferred CLI-wiring test gap (frontmatter `deferred:`); and a real, but out-of-this-story's-hands, condition — the active branch (`claude/bmad-build-autocomplete-5-10-6a79b6`) carries the auto-generated `claude/` prefix `AGENTS.md`'s own Policy section forbids for a branch name, to be corrected (a new, semantically-named branch) before this work is shipped as a PR, not by a change to this story's code or spec.
