---
topic: Claude Code / Agent SDK invocation mechanics for auricle AI stages
type: technical
decision: Whether auricle's AI stages (Epic 3 Summarize, diarization reviewer) should be invoked via a managed Claude Code session with file-based skill prompts, instead of an in-Swift SummarizationPromptBuilder + direct Anthropic API call
preset: standard
updated: 2026-09-16T18:43
---

- (claim) ref=[L1] status=verified class=behaviour pub=2026-09 — 'claude -p --bg' is rejected at exit code 1 with the message: '--bg and --print conflict: --print never starts the interactive session that `claude agents` attaches to, so the job would be unattachable.' Observed twice locally on 2.1.220. The binary itself states -p is unattachable by design.
- (claim) ref=[L2] status=verified class=mechanism pub=2026-09 — The -p --output-format json result object DOES carry explicit error signalling: keys observed are is_error, subtype, terminal_reason, api_error_status, stop_reason, permission_denials, session_id, num_turns, duration_ms, duration_api_ms, total_cost_usd, modelUsage, usage, uuid, result, type. Overturns the Q1 subagent claim that 'the JSON payload has no success or error field'.
- (claim) ref=[L3] status=verified class=behaviour pub=2026-09 — TRAP: on a failed run the result object had is_error=true while subtype='success' and terminal_reason='api_error'. Reproduced on two independent runs. A parent that branches on subtype would misread a failure as a success; is_error is the field that carries the signal.
- (claim) ref=[L4] status=verified class=mechanism pub=2026-09 — --session-id <uuid> lets the PARENT assign the session id before the run; the returned result echoed the assigned uuid exactly. Overturns the Q3 subagent claim that the session id is only available at completion and is therefore unavailable for resuming a killed run.
- (claim) ref=[L5] status=verified class=mechanism pub=2026-09 — Session transcript is written to ~/.claude/projects/<mangled-cwd>/<session-id>.jsonl. Line 'type' values observed: queue-operation, user, attachment, file-history-snapshot, assistant, last-prompt. No 'system' line type appeared in the sample; sample was a 7-line failed run so this is suggestive, not conclusive, on whether the system prompt is ever recorded.
- (claim) ref=[L6] status=disputed class=behaviour pub=2026-09 — Two independent 'claude -p' subprocess invocations from a non-interactive context both failed with 'Failed to authenticate: OAuth session expired and could not be refreshed', while the ambient interactive session remained authenticated. Whether this is general to subprocess invocation or specific to this machine's OAuth state is UNRESOLVED; note --bare documents auth as 'strictly ANTHROPIC_API_KEY or apiKeyHelper' and 'claude setup-token' exists for long-lived tokens.
- (claim) ref=[L7] status=verified class=mechanism pub=2026-09 — 'claude attach <id>' EXISTS in 2.1.220 but is NOT listed in 'claude --help' Commands. Verbatim usage: 'Open the background session in this terminal. <- returns to agent view, Ctrl+Z drops back to your shell. The session keeps running either way.'
- (claim) ref=[L8] status=verified class=version pub=2026-07 — GitHub issue anthropics/claude-code#80954 is REAL, opened 2026-07-24, and is CLOSED AS NOT PLANNED (not open, as the Q2 subagent reported). Title confirms '-p --remote-control is silently ignored'. Its closure is evidence Anthropic declined the request, which is different evidence than a pending one.
- (event) Lead-run local behavioural probe completed; results at scratchpad/probe/results.txt, copied into digests/. Probe overturned three subagent claims (Q1 error field, Q3 session id availability, Q2 issue status).
- (claim) ref=[L9] status=verified class=behaviour pub=2026-09 — 'claude --bg' IGNORES --session-id. Verbatim: 'warning: --bg manages the session id; ignoring --session-id (use --resume <id> to continue an existing session)'. The short job id is printed only as human-facing text on stdout. Overturns the Q2b subagent claim that --session-id pre-assigns ids for --bg.
- (claim) ref=[L10] status=disputed class=behaviour pub=2026-09 — agent-view.md instructs polling 'claude agents --json --all' and reading each entry's 'state', 'status' and 'waitingFor'. On installed 2.1.220 the observed field union across 7 entries was cwd/kind/name/pid/sessionId/startedAt/status only — no 'state', no 'waitingFor'. Documented-vs-observed discrepancy; both sides reported.
- (claim) ref=[L11] status=verified class=version pub=2026-09 — The live docs describe features requiring 2.1.221, 2.1.246, 2.1.257, 2.1.259, 2.1.261 and 2.1.265, all NEWER than the installed 2.1.220. The published docs run ahead of this install, so a documented feature is not necessarily present locally.
- (claim) ref=[L12] status=overturned class=security pub=2026-09 — All five CVEs surfaced by the Q4 red team (CVE-2025-55284 fixed 1.0.4, CVE-2025-52882 fixed 1.0.24, CVE-2025-59536 fixed 1.0.111, CVE-2026-21852 fixed 2.0.65, CVE-2026-46406 fixed 2.1.128) are patched at or below 2.1.128, strictly below installed 2.1.220. NONE are live. Citing them as current risk would be a false claim.
- (claim) ref=[L13] status=verified class=security pub=2026-09 — headless.md states: 'Without --bare, a -p session runs the hooks in a project .claude/settings.json and connects the servers in its .mcp.json, even in a folder you have never trusted. A -p session shows no workspace trust dialog and no per-server approval prompt.' This is a current documented behaviour, not a patched CVE, and is the real privacy-surface finding.
- (claim) ref=[L14] status=verified class=mechanism pub=2026-09 — headless.md documents SIGTERM -> exit 143, SessionEnd hooks run, bash process tree terminated, turn left unfinished with no result recorded, and: 'When you resume the session, Claude Code continues the turn that SIGTERM left unfinished.' Resume-after-kill IS supported. Overturns the Q3 claim that a killed run cannot be resumed.
- (claim) ref=[L15] status=verified class=mechanism pub=2026-09 — --max-turns is accepted by the 2.1.220 CLI but is NOT listed in claude --help. Discriminated by control test: an invented flag returns 'error: unknown option', while --max-turns does not. Real-but-hidden.
- (claim) ref=[5] status=verified class=security pub=2026-09 — permissions.md states verbatim that a Bash rule 'isn't a security boundary around the program', that managed settings are highest so 'no other level, including command line arguments, can override a managed permission rule', and that the read fence must be Set (auto mode 'offers to turn it on the first time Claude reads outside the working directories'), which establishes off-by-default. Quotes checked first-hand by the lead.
- (event) Semantic citation-fidelity pass complete: all load-bearing markers ([2],[12],[18]) SUPPORTED with exact quotes; [9] flagged PARTIALLY SUPPORTED for the word 'probabilistic' and reworded to 'not guaranteed / model's judgement'. [5] and [7] were not reached by that pass and were verified directly by the lead instead.
- (event) Finalize complete: mechanical citation check ok (0 dangling, 0 orphaned), semantic citation-fidelity pass ok, doc_standards skill:bmad-review lenses=structure,prose applied (structure: 12 of 14 findings accepted, 2 PRESERVE; prose: all named findings applied incl. US spelling, serial commas, ID/UUID initialisms, numerals, intensifier pruning). Verdict table moved above executive summary. Staleness recomputed via recon_kit; earliest recheck 2026-10-01.
- (claim) ref=[L6] status=overturned class=behaviour pub=2026-09 — The subprocess OAuth failure was LOCAL EXPIRED STATE, not a property of subprocess invocation. After the user re-authenticated, 'claude -p' as a subprocess returned is_error=false, subtype=success, terminal_reason=completed, result='pong'. The earlier disputed claim is resolved and must not be reported as a general constraint.
- (claim) ref=[L16] status=verified class=behaviour pub=2026-09 — Session transcript IS written during a run, not only at completion. On a 27s -p run the .jsonl was absent at t+0/2/4s, present at t+6s (34683 bytes), and grew to 42257 bytes by t+25s. Growth is chunked (2 growth events across 10 during-run samples), so external observation is possible but coarse-grained, not per-token. Overturns the round-1 'transcripts written post-completion' claim.
- (claim) ref=[L17] status=verified class=behaviour pub=2026-09 — PROVENANCE, settled by sentinel test: text passed via --append-system-prompt does NOT appear in the session transcript (0 occurrences across 11 lines; union of line keys contains no system-prompt field) and does NOT appear in a --debug-file log (0 occurrences in 311 lines / 38947 bytes of real debug output whose 7 'system' matches are all CA-cert and plugin-loader lines). No hash, length or token count of the system prompt is logged either. Q5 provenance upgrades from UNVERIFIABLE FROM DOCS to CONFIRMED NOT SUPPORTED (observed).
- (claim) ref=[L3] status=verified class=behaviour pub=2026-09 — Refined with the success case now observed: on SUCCESS is_error=false, subtype='success', terminal_reason='completed'; on FAILURE is_error=true, subtype='success', terminal_reason='api_error'. subtype read 'success' in BOTH, so it carries no signal at all. is_error and terminal_reason are the two discriminating fields.
- (claim) ref=[L18] status=verified class=behaviour pub=2026-09 — Observed cost without --bare: total_cost_usd 0.165 for a one-word 'pong' reply and 0.824 for a 50-line trivia list, both single-turn. The floor is context loading, not output. Client-side estimate per the docs. Relevant to any per-invocation cost ceiling.
- (claim) ref=[L10] status=overturned class=behaviour pub=2026-09 — WITHDRAWN: the r1 'documented-vs-observed discrepancy' on state/waitingFor does not exist. Fields split by session kind: background carries state (done/stopped/working/blocked/failed per docs), interactive carries status (busy/idle/waiting) plus waitingFor. r1's sample contained no running background session. The docs were accurate.
- (claim) ref=[L19] status=verified class=mechanism pub=2026-09 — --bg completion detection IS supported: agent-view.md names 'claude agents --json' as 'the supported way to read session state from outside Claude Code' with state enum working/blocked/done/failed/stopped, so a parent can distinguish success from failure. claude stop <id> kills by ID without a TTY.
- (claim) ref=[L20] status=verified class=mechanism pub=2026-09 — A --bg session's final result IS machine-readable in practice: ~/.claude/jobs/<id>/state.json carries state, detail, output.result (observed holding the exact model output), tokens, sessionId, resumeSessionId, cliVersion, backend. BUT the docs disclaim it verbatim: 'The files under ~/.claude/jobs/<id>/ are not a stable interface.' claude logs <id> is the alternative and returns raw ANSI terminal capture. So: available in practice, unsupported in principle.
- (event) Revision r2 complete. Re-probed after user re-authenticated. Closed open questions 1-5. Corrected three r1 findings (subprocess auth cause, state/waitingFor discrepancy withdrawn, --bg supervision understated). Upgraded Q5 provenance from UNVERIFIABLE FROM DOCS to CONFIRMED NOT SUPPORTED via sentinel experiment. Citations ok, PII clean.
- (claim) ref=[L21] status=verified class=mechanism pub=2026-09 — A Stop hook FIRES on --bg turn completion and its payload carries last_assistant_message holding the exact model output (observed 'ping'). Documented: 'Hooks that need the final assistant text of the current turn should use last_assistant_message on Stop and SubagentStop instead of reading the transcript.' This gives --bg a documented result channel and closes the r2 'result payload has no supported channel' gap.
- (claim) ref=[L22] status=verified class=mechanism pub=2026-09 — SessionEnd does NOT fire on --bg turn completion. Observed: only Stop fired at state=done; SessionEnd fired only after 'claude stop', with reason='other'. Matches documented semantics ('When a session terminates') since a --bg session at state=done is idle awaiting input, not ended.
- (claim) ref=[L20] status=overturned class=mechanism pub=2026-09 — SUPERSEDED: the r2 claim that a --bg result is 'available in practice, unsupported in principle' is wrong. The Stop hook's last_assistant_message is a documented, recommended channel. Reliance on the disclaimed state.json is unnecessary.
- (event) Revision r3 complete: hook lifecycle probed. All six r1 open questions now closed. Central trade-off reduced from 'attachable XOR parseable' to 'ID scrape plus push-vs-return plumbing'.
