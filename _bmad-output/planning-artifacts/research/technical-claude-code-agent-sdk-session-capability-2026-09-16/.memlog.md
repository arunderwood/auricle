---
topic: Claude Code / Claude Agent SDK session capability surface
type: technical
decision: Epic 3 AI invocation architecture: direct Messages API from Swift vs managed Claude Code / Agent SDK session
preset: standard
updated: 2026-09-16T18:32
---

- (decision) Plan approved: technical/explore, breadth-first, 3 researchers (A=Agent SDK session surface, B=Messages API primitives for citations/structured-output/cache_control, C=billing+auth+usage JSON shapes). Preset standard (3 subagents, 8 sources/round, depth 2), validation normal, red_team off. User chose docs-only for Q2 offset semantics: if docs are silent the verdict is UNVERIFIABLE FROM DOCS, no empirical API probe.
- (source) Round 1 assistant A (Agent SDK surface) returned 13 claims across code.claude.com (headless, costs, authentication, setup, prompt-caching, agent-sdk/structured-outputs) and platform.claude.com (citations). Digest: digests/agent-sdk-surface-r1-1.md
- (source) Lead first-hand fetch: code.claude.com/docs/en/authentication. 5 claims on billing/credential precedence, all direct-quote grounded. Digest: digests/billing-auth-r1-lead.md
- (claim) ref=[auth] status=verified class=capability pub=2026-09 — A programmatically driven Claude Code / Agent SDK session bills to either an API key or a Claude subscription; selection is a documented 7-step credential precedence order, and in non-interactive mode a present ANTHROPIC_API_KEY always wins.
- (source) Lead first-hand fetches: code.claude.com/docs/en/agent-sdk/cost-tracking and /docs/en/headless. 14 usage claims, 4 caching claims, 3 structured-output claims, 2 auth corroborations. Digest: digests/usage-cost-r1-lead.md
- (claim) ref=[usage] status=verified class=capability pub=2026-09 — Per-step usage is exposed on assistant messages (input/cache tokens accurate, output_tokens a documented placeholder); per-call totals plus a model-keyed modelUsage breakdown (costUSD, inputTokens, outputTokens, cacheReadInputTokens, cacheCreationInputTokens, costBasis) are on the result message.
- (claim) ref=[cost-estimate] status=verified class=capability pub=2026-09 — total_cost_usd and costUSD are client-side estimates computed from a price table bundled at SDK build time; docs say 'Do not bill end users or trigger financial decisions from these fields.'
- (claim) ref=[cache-control] status=verified class=capability pub=2026-09 — Agent SDK caching is automatic ('You do not need to configure caching yourself'); documented host control is TTL-bucket-level (ENABLE_PROMPT_CACHING_1H, promptCacheTtl, subagentPromptCacheTtl) with two buckets, not per-content-block cache_control placement. Cache hits observable via cache_read_input_tokens / cache_creation_input_tokens.
- (question) Does any declared Usage type field report thinking/reasoning tokens separately? cost-tracking.md never mentions one but is not an exhaustive type listing. Routed to round-2 type-reference assistant. Do not verdict from the prose page alone.
- (event) Round-2 type-reference assistant returned contradictory type extractions (SDKResultMessage rendered as a tool-result block; ModelUsage rendered snake_case without costUSD). Conflicts resolved in favor of code.claude.com/docs/en/agent-sdk/cost-tracking per source-quality card (worked examples naming fields + direct anchor links). Digest quarantined as LOW RELIABILITY; lead re-verifying the load-bearing items first-hand.
- (source) PyPI claude-agent-sdk v0.2.153, updated 2026-09-15 (registry primary source). npm @anthropic-ai/claude-agent-sdk returned 403 to automated fetch; npm version unverified.
- (source) Lead round-2 authoritative probe: npm @anthropic-ai/claude-agent-sdk v0.3.274 (registry modified 2026-09-17T00:12Z), read shipped sdk.d.ts (9368 lines) and sdk-tools.d.ts (4184 lines) directly. Digest: digests/sdk-typedefs-r2-lead.md. Overturned 5 prior subagent claims.
- (claim) ref=[usage] status=verified class=capability pub=2026-09 — OVERTURNS earlier caution: ModelUsage declares thinkingTokens?: number (already counted inside outputTokens; only on turns run by CLI versions that record it; partial for sessions resumed from older versions), plus costUSD, canonicalModel, provider, contextWindow, maxOutputTokens, webSearchRequests.
- (claim) ref=[cache-control] status=verified class=capability pub=2026-09 — cache_control/cacheControl appears ZERO times across 9368 lines of shipped sdk.d.ts; systemPrompt accepts only string | string[] | {type:'custom'} | {type:'preset'}, no structured content-block array, so no position exists at which a host could attach a per-block breakpoint. Only TTL selectors promptCacheTtl / subagentPromptCacheTtl are exposed.
- (claim) ref=[citations-sdk] status=verified class=capability pub=2026-09 — SDKUserMessage.message is an Anthropic MessageParam whose shipped comment names 'document' among permitted block types, so the input type structurally permits a citations-enabled document; but the SDK's own type surface contains no citations feature (zero genuine citation identifiers in sdk.d.ts; the single hit in sdk-tools.d.ts is an untyped unknown[] on the Agent tool report). End-to-end behavior is undocumented.
- (claim) ref=[citations-offsets] status=verified class=capability pub=2026-09 — Messages API docs never state the unit of start_char_index/end_char_index (bytes/UTF-16/codepoints/graphemes) nor any server-side Unicode normalization; corroborated by named negative searches across 5 surfaces. end_char_index has no description at all in the API reference schema.
- (claim) ref=[citations-vs-structured] status=verified class=capability pub=2026-09 — Citations and structured outputs are documented as mutually exclusive at the Messages API layer: enabling citations on a document or search_result block together with output_config.format (or deprecated output_format) returns a 400 error. Documented on the citations page only; the structured-outputs page does not mention it.
- (event) research.md assembled: 5 verdicts (2 CONFIRMED AVAILABLE, 1 CONFIRMED UNAVAILABLE for per-block cache_control with observability available, 2 UNVERIFIABLE FROM DOCS on both halves of citations), plus cross-cutting findings, constraint mapping, 7 open questions, 13-source appendix, staleness map. No recommendation section per the user's explicit output contract (overrides the skill template's section 5).
- (event) Semantic citation check (fresh-context verifier, 6 load-bearing quoted claims not verified first-hand by the lead): ALL SIX VERIFIED. Two precision corrections applied to research.md — (a) cache-read 0.1x is qualified 'see the table footnote for per-model exceptions' in the source, not flat; (b) the 'byte' search term does return 6 matches in the citations guide, all inside SDK code samples reading a PDF off disk, none defining an index unit. The Q2b offset-unit absence claim was independently re-run and holds.
- (event) Built two empirical probes into the research workspace: citation_offset_probe.py (open question #2, needs ANTHROPIC_API_KEY, not yet run) and session_citation_probe.py (open question #1). Ran the session probe 2026-09-16: PROBE INVALID — the machine's Claude Code OAuth login has expired ('Failed to authenticate: OAuth session expired and could not be refreshed'), so document run and control failed identically. No finding claimed. The control run is what prevented a false 'document block dropped' conclusion. Re-run after claude /login.
- (assumption) Probes conform to the project's python-scripting standard (PEP 723 inline deps, typer, loguru, documented exit codes 0/1/3) and share probe_common.py so both index the identical document — if the session probe ever surfaces citations, its offsets are directly comparable against the direct-API probe without re-deriving anything.
- (claim) ref=[P1] status=verified class=measured-behavior pub=2026-09 — Claude Code session DOES forward a citations-enabled plain-text document block and the full char_location object reaches the host, but ONLY as a citations_delta stream event under --include-partial-messages; the reassembled assistant message carries citations:[] (key present on the cited block, payload not folded in). CLI 2.1.220, claude-fable-5.
- (claim) ref=[P2] status=verified class=measured-behavior pub=2026-09 — Citation start_char_index/end_char_index are UNICODE CODEPOINTS counted against the text AS SUBMITTED, with no server-side NFC normalization. Sole matching unit on two inputs with differing predictions (NFC prefix -> 53, NFD prefix -> 57); slicing the NFD result against an NFC-normalized copy matched nothing.
- (claim) ref=[P3] status=verified class=measured-behavior pub=2026-09 — CLI 2.1.220 modelUsage omits thinkingTokens and costBasis while carrying the other ten declared fields, empirically confirming both version gates read from the shipped .d.ts (costBasis requires v2.1.246+).
- (event) Open questions 1, 2 and 3 CLOSED by measurement. Verdict table rows 2a/2b now carry both the documentation verdict (still UNVERIFIABLE FROM DOCS) and the observed behavior, kept explicitly distinct. Raw stream captures archived in evidence/.
- (claim) ref=[P4] status=verified class=measured-behavior pub=2026-09 — The empty citations:[] is deliberate: Claude Code 2.1.220's own stream accumulator contains an explicit 'case "citations_delta": break;' no-op, while the Anthropic SDK accumulator bundled in the same binary folds the payload in correctly. Evidence from strings -a over the shipped executable.
- (claim) ref=[14] status=verified class=capability pub=2026-09 — The Messages API streaming page documents text_delta, input_json_delta, thinking_delta and signature_delta but contains zero occurrences of 'citation', so citations_delta is undocumented on the one surface that actually carries citation offsets to a host.
- (claim) ref=[P5] status=verified class=measured-behavior pub=2026-09 — A session returns schema-constrained structured_output AND char_location citations in one call with no error, though the Messages API documents that combination as a 400. Offsets identical to [P2]. Explained by the session using an end-turn tool rather than output_config.format.
- (claim) ref=[P6] status=verified class=measured-behavior pub=2026-09 — All three citation location types traverse a session: plain text -> char_location, PDF -> page_location (1-indexed, cited_text carried CRLF and did not byte-match the source), custom content -> content_block_location (0-indexed block indices, no character offsets involved).
- (claim) ref=[P7] status=verified class=measured-behavior pub=2026-09 — The session's structured output did not inherit two documented Messages API schema limitations: minLength:40 was respected and a recursive $ref was accepted. Mechanism (constrained decoding vs retry) undetermined; minLength needed more than one turn.
- (event) Category-A probe round complete: open questions 4, 5 and 8 closed by measurement via capability_probes.py. Cross-cutting finding #1 CORRECTED — the citations/structured-output incompatibility is real at the Messages API layer but absent at the session layer [P5]. Three new residuals logged (minLength mechanism, prompt-instruction JSON route, tool-result location types).
