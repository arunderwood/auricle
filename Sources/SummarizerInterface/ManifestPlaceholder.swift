// Empty on purpose: SwiftPM refuses to build a target that is exposed as a
// library product but contains zero source files. Real code lands target by
// target starting with Story 1.2; delete this file once that target does.
